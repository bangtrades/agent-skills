# {ENTITY_NAME} Visual Tokens

Extracted from {primary_url} via the firecrawl `branding` format on {DATE}. Authoritative for any deliverable.

## Color Palette

### Primary Brand Colors

| Token | Hex | Use |
|---|---|---|
| `{token-1}` (primary) | `{hex}` | {role description} |
| `{token-2}` (secondary) | `{hex}` | {role description} |
| `{token-3}` (accent) | `{hex}` | {role description} |
| `{token-4}` (background) | `{hex}` | {role description} |
| `text-primary` | `{hex}` | Body copy default |
| `text-inverse` | `{hex}` | Body copy on dark surfaces |

### Supporting Tones

| Token | Hex | Use |
|---|---|---|
| `{tone-1}` | `{hex}` | {use} |
| `{tone-2}` | `{hex}` | {use} |
| `{tone-3}` | `{hex}` | {use} |
| `{tone-4}` | `{hex}` | {use} |

### Functional Colors

| Token | Hex | Use |
|---|---|---|
| `success` | `{hex}` | {use} |
| `warn` | `{hex}` | {use} |
| `danger` | `{hex}` | {use} |

## Typography

### Font Families

- **Primary:** {Font Family} — Used for {usage}
- **Secondary:** {Font Family} — Used for {usage}
- **Fallback stack:** `{Primary}, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`

### Type Scale (web)

| Level | Size | Weight | Letter-spacing |
|---|---|---|---|
| Display | {size} | {weight} | {ls} |
| H1 | {size} | {weight} | {ls} |
| H2 | {size} | {weight} | {ls} |
| H3 | {size} | {weight} | {ls} |
| Body | {size} | {weight} | {ls} |
| Small | {size} | {weight} | {ls} |
| Eyebrow / Label | {size} | {weight} | {ls} (uppercase) |

### Type Use Rules

- {rule about eyebrow/uppercase usage}
- {rule about heading color}
- {rule about body contrast}
- {rule about numbers/stats treatment}

## Spacing & Layout

- **Base unit:** {Npx} (everything is a multiple of N)
- **Grid:** {columns}-column on desktop, {columns}-column on mobile
- **Section vertical rhythm:** {Npx} desktop, {Npx} mobile
- **Card padding:** {Npx}
- **Max content width:** {Npx}

## Components

### Buttons

**Primary button** (call-to-action):
- Background: {hex}
- Text: {hex}
- Border radius: {value}
- Padding: {value}
- Hover: {value}

**Secondary button:**
- Background: {hex}
- Text: {hex}
- Border: {value}
- Border radius: {value}
- Padding: {value}

**Tertiary / text link:**
- Color: {hex}
- Underline on hover
- No background

### Cards

- Background: {hex}
- Border: {value}
- Border radius: {value}
- Shadow: {value}
- Padding: {value}

### Inputs

- Background: {hex}
- Border: {value}
- Border radius: {value}
- Padding: {value}
- Focus border: {hex}

### Icons

{Note the icon system in use — Material Symbols, Heroicons, Lucide, custom, etc. — and any size/color conventions.}

## Logo Usage

- **Style:** {wordmark / lockup / icon-mark / wordmark+icon}
- **Color:** {usage rules — when light, when dark}
- **Clearspace:** {minimum padding}
- **Minimum size:** {px / inches}
- **File reference:** {URL}

## Photography & Imagery Style

- {observation 1 about the photographic style — staged vs. candid, location, models, lighting}
- {observation 2}
- {observation 3}
- {observation 4 — what to avoid}

## Accessibility

- {Body text contrast check on primary background}
- {CTA button contrast check}
- {Link color contrast check}
- {Other accessibility considerations specific to the brand's visual system}

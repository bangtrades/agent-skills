# RETRO — {Client} build ({date})

*Mandatory closeout per client-build-package Step 5. Runs at demo-complete AND at engagement
close. Fill from hard evidence — Linear `startedAt`/`completedAt`, artifact mtimes, commit times —
not memory. Open a Linear issue for this retro and move it to In Progress before you start it.*

**Wall-clock:** {start → demo, HH:MMm}. **Motion:** {SMB/DTC · enterprise · white-label}.
**Scope:** {slices run}.

## 1 · Timeline (planned vs actual)

| phase / slice | planned | actual start | actual end | wall-clock | notes |
|---|---|---|---|---|---|
| S1 Recon | | | | | |
| S2 Position + demo-spec | | | | | |
| S3a Sim contract | | | | | |
| S3b–e Surfaces | | | | | |
| S3f Integration/calibration | | | | | |
| S3h QA + script | | | | | |
| (estate / other) | | | | | |

**Single biggest wall-clock lever this run:** {name it, with the minutes it cost or saved}.

## 2 · What compounded (keep / strengthen)

- {reusable wins — evidence-based tie-outs, vault schema-library hits, contract-first, per-checkpoint commits, brand skill reuse…}

## 3 · What slowed it (root-caused, with cost)

- {failure → root cause → wall-clock cost → the fix, and whether it's now a skill rule}

## 4 · Board-discipline self-check

- Did **every** issue move to In Progress *before* its work started? {yes/no}
- Issues that jumped created→Done (never In Progress): {list, or "none"}
- Fix applied to the habit (not just the record): {…}

## 5 · Skill diffs (every skill the engagement used)

For each of brand-recon · data-simulator · client-build-package · orchestration (and any format
skills), list concrete diffs — new patterns in, disproven guidance out. Mark each **PATCHED
in-session** (writable) or **staged in `00_context/skill-patches/`** (read-only install).

| skill | diff | status |
|---|---|---|
| | | |

## 6 · Run-log entry

Append the dated one-paragraph summary to `~/Projects/agency/playbook/00-retrospective-2026-07.md`
(or `RUN_LOG.md` beside the manifest): actual vs planned timeline, what landed, what broke,
pricing/objection notes, the single biggest lever.

## 7 · Standing state / next natural phase

- Gates: {G1/G2 status}. Board: {all Done? open issues?}.
- Next phase when the operator wants it: {…}

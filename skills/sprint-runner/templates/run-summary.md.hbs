# {{story_id}} — {{story_title}} Run Summary

**Sprint:** {{sprint_n}}
**Story:** {{story_id}} ({{pts}} pts)
{{#if retires}}**Retires:** {{retires}}
{{/if}}**Landed:** {{landed_date}}
**Status:** {{status}}

<!-- FILL: Opening paragraph — 2–4 sentences. What's the context? What prior runs does this build on? Why did this story matter to the sprint goal? Link prior runs as `[S{{sprint_n}}-XX](./SN-XX-slug.md)`. -->

## What shipped

{{#each files_changed}}
- `{{path}}` ({{status}}, {{loc_delta}})
{{/each}}

<!-- FILL: 1–2 sentences per cluster of files. Group by module if there are more than ~8 files. Annotate any file that's new with (new) and any test file with its test-count delta like (+7 tests, 23 → 30). -->

## Architectural decisions

<!-- FILL: Numbered list. Every decision must answer "why X over Y". If there are no non-trivial decisions (rare), delete this section entirely rather than padding it.

Format:
1. **<Decision>** — <What was chosen>. <Why it was chosen over the obvious alternative>. <Trade-off accepted>.
2. ...
-->

## Quick-start runbook (bang's Mac)

```bash
<!-- FILL: Numbered bash steps with `# expect:` annotations. Example:
cd ~/Projects/Novai/src/Backend
npm run dev
# expect: Server listening on :3001 with Sparky skill-engine hooks loaded

curl -s localhost:3001/health | jq
# expect: { "status": "ok", "skillEngine": "loaded" }
-->
```

## Validation matrix

| Check | Where | Status | Notes |
|-------|-------|--------|-------|
<!-- FILL: One row per validation step. Use ✅ for sandbox-validated, 🟡 for Mac-only (requires bang to run). Be explicit about what ran where. -->

{{#if has_mac_only}}
### Prerequisites bang must run on his Mac

```bash
<!-- FILL: Bash block for anything that couldn't run in the sandbox — DB migrations, long-running tests, manual Dev Console smoke tests, etc. -->
```
{{/if}}

{{#if has_ui}}
## End-to-end Dev Console walkthrough

<!-- FILL: Numbered steps a tester would follow in the Dev Console UI. Include expected state transitions and any gotchas. Delete this section if the feature has no UI surface. -->
{{/if}}

{{#if has_http_api}}
## curl cheat sheet

```bash
<!-- FILL: Real curl commands piped through jq with # expected: annotations. Example:
curl -s -X POST localhost:3001/api/skill/execute \
  -H 'content-type: application/json' \
  -d '{"skillId":"echo","args":{"msg":"hi"}}' | jq '.result'
# expected: { "echoed": "hi" }
-->
```
{{/if}}

{{#if has_flags}}
## Feature-flag matrix

| Flag | Default | Effect when on | Effect when off |
|------|---------|----------------|-----------------|
<!-- FILL: one row per flag touched -->
{{/if}}

{{#if retires}}
## Debt retired

<!-- FILL: For each carry-in item this run closes, a short section:

### {{retired_id}} — <Title>
<1–2 sentences on what was wrong and how this run fixed it.>
-->
{{/if}}

## What's next

<!-- FILL: 1–3 bullets on the next logical step. Don't over-commit — this is a preview, not a plan. -->

## Cross-references

<!-- FILL: Links to prior run summaries, related ADRs, external docs. Example:
- Prior run: [S{{sprint_n}}-XX](./SN-XX-slug.md)
- Related ADR: [../adrs/NNNN-title.md](../adrs/NNNN-title.md)
-->

---

*Generated by sprint-runner skill. Git range: `{{from_sha}}..{{to_sha}}` ({{commit_count}} commits, {{loc_total}} LOC).*

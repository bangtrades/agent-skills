# ENTRY 2026-09-11 — Acres (acres.com) investigation + canonical BT Stock Report

**Shape.** Firecrawl capability probe (scrape/map/search/agent all green) → 6 disjoint Opus research slices (corporate-legal, leadership, funding, product-tech, market-comps-macro, sentiment-social), each write-first, single-file ownership, two-citations-per-fact, four-tier source trust → 1 read-only adversarial Opus QA gate (19 facts re-fetched) → 4 fix-round agents with findings verbatim (Law 9 reproduce-first; 4 refutations recorded) → orchestrator signed-in Chrome pass (LinkedIn company/people/experience pages; Arkansas SOS; SEC Form D primary docs) → Opus synthesis (dossier + acres-brand skill + _playbook/_sources/_runs + log) → orchestrator-authored BT report (docx-js → LibreOffice PDF, 9 pages) → Opus final gate → fixes → release.

**What moved the project.** The GET-URL probe of a registry everyone had called a POST form; reading the unread Form D notices; the VP Engineering LinkedIn experience page (the richest technical/unit-economics disclosure anywhere); running the gate on the orchestrator's own draft.

**Mis-scopes.** The brief's premise ("spun out of AcreTrader") was inverted; the slices corrected it because the brief said "verify orchestrator-supplied facts." Slice 03 initially reported "no corporate Form D" — a search-artifact negative that slice 01's CIK-specific claim and the fix round overturned (catalog over error message).

**Environment.** `/sessions` full → npm ENOSPC → build in `/tmp`. Brand font absent in sandbox → fontconfig alias. LinkedIn auth wall in the first Chrome profile; do not enter credentials — user signed in / connected the right profile; `switch_browser` broadcast never fired but the device list stayed "Browser 1" and simply started serving the signed-in profile.

**Deliverables.** `Reports/equities/2026-09-11-acres-bt-stock-report.pdf` + `2026-09-11-acres.md`; `research/brand-recon/acres/{dossier.md, acres-brand/, raw-slices/01–08, raw-scrapes/, report-build/build_report.js}`; indexes + log updated.

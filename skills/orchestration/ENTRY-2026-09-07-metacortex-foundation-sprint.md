# Foundation sprint orchestration retrospective

Run: `mc-foundation-sprint-20260907-01`. Nolan authorized GPT-5.6 Sol developers, separate Sol QA, root verification and code publication. The original eight primary and four conditional issues remain the acceptance contract; human owner appointments, signed policy authority and production evidence are distinct from local code approval.

## What changed the result

- Independent DB QA reproduced effective access through child-role grants and inherited ownership. The same producer repaired the catalog-derived postcondition while retaining every legacy checkpoint row. Real predecessor functions and real narrow caller roles were necessary; a simplified substitute function would not establish the privilege contract.
- CI QA rejected hardcoded module selection and a permissive fixture function. Root combined-tree verification then found non-suffix tests using PostgreSQL fixtures that would escape mandatory CI. Selection now covers direct and transitive fixture use. Both PostgreSQL 16 and 17 execute 46 required combined cases without skips at `efc76ef`.
- Access QA found a preparation command outside the first TLS inventory. The repair routes the actual checkpoint preparation path through the same verified connection helper. Root CI-shaped execution also caught a namespace import and an absolute external checkout dependency that producer-root tests had hidden; fresh QA approved the bounded portability fix.
- Release QA rejected a qualification harness whose command and expected output could be fabricated. The repaired contract binds actual signed release trees, full policy objects, public-key fingerprints and the real service entrypoint. A later shutdown probe showed that bounding an asyncio await does not bound the lifetime of its synchronous worker thread. The custom FastAPI/synchronous-saver receipt remains partial evidence, never a substitute for the real PID-1/async-checkpointer recovery drill.
- Fresh live reads corrected historical deployment assumptions: current uvicorn is already PID 1, the signed v3 mount agrees with all 19 full database gate definitions and five budget terms, and the retained rollback artifact can be verified. None of these observations establishes that the new candidate has been deployed.

## Orchestrator corrections

The initial readiness account carried stale runtime and artifact premises; an explicit evidence addendum preserves and corrects them. Migration provenance was initially overstated and was changed to deployment/ratification unverified before publication. Root's first integrated test invocation failed from the actual CI directory; the failed logs are retained and the fixed candidate was rerun. Several exploratory reads guessed paths instead of enumerating them first; use scoped `rg --files` before opening an uncertain filename.

The shared main checkout and Cortana index contained unrelated work. Development used isolated worktrees; Cortana revisions used per-path transformations against separately preserved worktree, index and HEAD content, temporary commit indexes, normal hooks and before/after foreign-staging checks. No global add, stash, reset or worktree prune was used.

## Later root verification findings

The first long-lived controller connection froze PostgreSQL `now()` at its initial transaction, so the lease-expiry wait could never observe time advancing. A real PostgreSQL reproduction distinguished transaction time from `clock_timestamp()`; the same producer changed the polling connection and predicate. Full controller rehearsal must precede a handoff, and fixtures must remain available until independent QA finishes.

A schema-only export with no ACLs left default PUBLIC function execution in place even after table permissions matched. Root compared all six role attributes, three memberships, 81 table-permission records and 78 function-permission records against fresh read-only production metadata. The failed parity receipt remains evidence; table-only checks cannot establish a narrow capability profile.

Hosted validation at `59414fc` exposed concurrent manifest mutation during `deepcopy` after a clean local suite. Preserve that exact failed log, reproduce the race deterministically, repair it in an isolated worktree and require separate QA plus fresh hosted validation. Re-running a nondeterministic failure until green does not close it.

The final identity reader initially labeled the real-ID column from `/proc/1/status` as effective. Root corrected the two indexes, tested a disposable process with deliberately different real/effective IDs, and obtained separate QA verification. Root then reran the complete corrected controller. Equal IDs in ordinary containers would have hidden that semantic error.

## Skill disposition

No new numbered law is needed: the existing exact-composition, real-role, independent mutation, quiet-tree, provenance and three-way skill-lineage rules already cover the observed failures. Append a concise run entry to the current richer staged skill, preserve concurrent additions, and leave registry publication to its normal gate. The final receipt will bind the resulting package and actual completion limits.

Final outcome and exact release/hosted-CI identities are recorded in the successor acceptance report and Cortana revision; this retrospective does not assert completion of pending production or human-owner gates.

## Changelog

- **2026-09-20 · Lineage reconciliation onto the be33df6 slim structure (orchestrator-performed; no publish).** Reconciled the 09-14 staged lineage onto the be33df6 slim structure; six patches applied (09-06 production cutover, 09-06 Slack live, 09-10 Firecrawl Research Index, 09-14 sector-theme frontmatter contract, 09-17 research-hub registration, 09-19 taste-branch frontmatter contract — folded into Operator-Runbook Execution, Research-Fleet Discipline and a new Research Slice template in `references/slice-templates.md`); 32 retro entries appended (the staged inline changelog's DetailAI/BT Journal lineage and 09-06→09-14 runs, plus the never-folded ENTRY files for 08-04 WAI-157, 08-12 WAI-48, 09-15, 09-16, 09-17, 09-18 and 09-19); the 09-03b entry took the staged post-merge addendum; the 09-08 S5 and 07-28 agent-registry runs each keep two same-run variants side by side (registry-condensed and staged-lineage; distinct titles, deliberately not collapsed); body rules from 09-07..09-19 folded into Laws 4/12, pipeline step 3, evidence custody, Research-Fleet, Sandboxed-Shell and Anti-Patterns; debris removed (SKILL.md backups, superseded/partial forks, RECOVERY note) to `_inbox/skills/_published/orchestration-debris-2026-09-20/`. Entries are now ordered newest-first by date. Slim SKILL.md structure retained per be33df6.

- **2026-09-19 · Vault cleanup wave (5 Opus content slices by path ownership + 1 micro-round + 1 Opus read-only adversarial gate + orchestrator fix round; Fable orchestrator; ~50 min; no git).** Nine memory close-out runs' operator-free flags; gate verdict 4 PASS / 2 PASS-with-defects / 1 FAIL (P1) → fixed; see the `[2026-09-19] orchestration` entry in `log.md`. Three additions. **(a) Timestamp provenance is a gate attack, not a spot-check.** A session-window heading in a close-out ("15:52–15:57 — Apex demo account") was the audit *file's* mtime; one hop later the hub and two promoted memories asserted "approved at 15:57 PDT" as a business event no audit recorded. Content-vault waves whose evidence class is "source-reported from documents" must grep every asserted timestamp back to a machine-readable stamp *inside* a cited document; a time that exists only in `stat` output or a heading is not a claim any source makes (SCHEMA: mtime is never evidence). Law 12 for prose: the gate reads the source, not the summary that cites it. **(b) Parallel slices that write cross-referencing facts need a seam gate, same as code.** Slice A revised two memories to say "the hub block is stale — read the daily page" while slice B, in the same wave, refreshed that block; both correct against their briefs, wrong together. When one slice's output is a *pointer* to state another slice changes, gate the pointer after both land, or serialize them. **(c) A tool that writes a fixed body makes every post-write append invisible to its own receipt.** `memctl remember` journals the sha of the text it wrote; the workflow then hand-appends a `## Review` section, so all ten receipts are stale and the anti-tamper column is decorative. Extend the tool (proposed `--review-file`) or stop claiming the receipt proves integrity — never both. Retro notes: ownership-by-path parallelism worked with zero collisions (08-08b confirmed) and the gate's `find -newermt` sweep is the load-bearing control — keep the start-time stamp; the Agent tool ran the first slice alone because it was dispatched in its own message (parallel dispatch needs all invocations in one message, ~6 min lost); QA's "heading arithmetic does not close" finding was a counting error in the orchestrator's own log entry — Law 9 applied in the fix round.

- **2026-09-18 · Taste deep wave (5 Opus research slices + blind rater 2 + adversarial QA + fix round + re-gate; Fable orchestrator; ≈143 Firecrawl credits; E FAIL → PASS-with-defects).** Program: `cortana-vault/research/taste/`; see `ACCEPTANCE-REPORT-2026-09-18.md`. Three additions. **(a) Law 12, reviewer-side corollary — a score that moves when the reviewer looks at less is grading the review, not the artifact.** The rubric slice introduced an "N/V" (not verified) state that removed a row from both numerator and denominator; blind rater 2 verified *less* than rater 1 (no render → five Design rows N/V) and filed 93/100 on the artifact rater 1 scored 78. The fix round's rule — N/V scores 0 and keeps its full weight in the maximum, coverage reported separately, verdict capped Provisional — is the only monotone shape. Any scoring instrument built by a wave gets an adversarial case where the reviewer withholds effort; the re-gate found the fix had reopened the hole through the N/A applicability table — applicability must be a property of the artifact class, never of the review. **(b) Parallel slices that cite each other's pages by section number will be stale by assembly.** Slice A inserted a new §2; slice B cited "fluency (§2)", "Bourdieu (§5)" six times from the pre-wave numbering and the drafts carried the error into the assembled canon. Cite by heading text or anchor, never by ordinal; grep `§\d` across the assembled tree before the gate and fix the section *drafts* in the same edit. **(c) Retrieval cost is per-surface, not per-call — brief the expensive surfaces by name.** One `firecrawl_scrape` of an x.com post billed 30 credits (75 % of that slice's budget) while a Substack page embedding the same tweet cost 1; `queryOptions.directQuote` failures still bill 5 each; screenshots return as signed URLs the agent cannot see, so "score from a render" was unfulfillable by every agent. The preflight capability probe records cost per surface, and briefs carry a per-call cost ceiling with the cheap corroboration route named. Confirmations: write-output-first + ~25-action budgets returned every slice with files on disk; Opus on the gate paid again (only the fresh adversarial pass found score inflation across all five calibration files); Law 9 held (the fix agent reproduced all four P1s numerically before editing); an orchestrator micro-round after a narrow re-gate was the proportionate close. Environment: `rmdir`/`unlink` blocked on the mount; `SendMessage` unavailable in Cowork, so fix rounds go to fresh agents with the QA report as the findings file (confirms 09-05).

- **2026-09-17 · Taste research branch, wave 1 (claude.ai chat, single orchestrator, no sub-agents).** Environment lesson: in the claude.ai chat surface, `firecrawl_agent` (autonomous research runs) requires an interactive approval that may never arrive ("No approval received"), while authenticated `firecrawl_search` and Tavily work without approval. Probe the agent surface FIRST with a trivial prompt; if it fails, fall back to search-per-slice run by the orchestrator, and say so in the acceptance report — do not narrate a fleet that did not run. Second lesson: a chat session without filesystem access to the vault must deliver a **package + PATCHES.md** (copy commands, index/log lines, verify steps) rather than claim vault writes. Third: one primary full-text fetch (Hume) cost ~9k tokens of context; for primary texts prefer `scrapeOptions.formats: ["summary"]` or targeted `queryOptions` and keep the full text out of the orchestrator's window. **Same run, Law 7c confirmed on vault frontmatter:** all six pages were written with bare `[[wikilinks]]` inside YAML `related:`/`sources:` lists. A grep for the keys would have passed; `yaml.safe_load` rejected every file. Rule for vault writers: wikilinks inside frontmatter must be quoted strings (`related: ["[[a]]", "[[b]]"]`), and the frontmatter gate loads YAML, never greps.

- **2026-09-16 · Mezzanine "The Arrival" v3 remake (orchestrator Higgsfield media across 3 rounds + 1 Opus scaffold dev + 3 Opus media agents + 3 Opus engine/fix agents + 1 read-only Opus adversarial gate; 600 of 700 credits; 0 P0, 6 P1 all closed; regress 94 → 174 checks, green on an independent build).** Four learnings. **(1) Self-gates written against stand-in media grade the engine, not the product.** The scaffold shipped 94/94 on v2 frames; the first real plate then failed 14 checks, and the QA gate later found three of four artworks cover-cropped to 28–50 % while "every quad inside the plate" stayed green. Rule: for composited media the gate asserts a viewer-facing property — fraction of the picture shown ≥ 0.98 and the artwork's on-screen bbox inside the viewport — at every size, and the adversarial gate runs only after real media has landed; a green gate on placeholders is a claim about placeholders. **(2) When a transform maps content onto a target, assert the content, not the container.** The v2 homography mapped the quad's *bounding box* onto the quad, so a 4:5 picture on a foreshortened side wall was squeezed and `object-fit: cover` hid it as a plausible crop (the same disguise as Sprint 3's `height=""` finding). The fix was one line — give the source rect the texture's own aspect — but no gate could see it because the assertion was "quad matches"; mutation-tested afterwards, reverting `cover` did not go red while reverting the source rect did, so the load-bearing assertion is the geometric one. **(3) Put the quantitative usability rule in the human-gate question, or measure before asking.** bang picked the Cinema Studio dolly from contact sheets ("most travel"); under the rule "all four painted panels inside the picture with a 1 % margin" it yielded 15 usable frames, Seedance 26, Gemini 41 — the tracker's number overrode the pick and honoured the stated intent ("the first ~2.5 s"), but the round cost a re-generation. Also from the media side: record `model_reported` and the rendered size alongside `model_requested` (Nano Banana Pro was served as `nano_banana_2`; Cinema Studio 3.0 reported 1344×768 upscaled to "1080p"), preflight estimates undercount 4K stills by ~1.6×, and Gemini flagged an empty-room portrait take `nsfw` — a reworded resubmit passed. **(4) The orchestrator freed disk by deleting a tool cache it later needed.** `/tmp/pwb` (full Chromium, 393 MB) went to make room for an independent build; Lighthouse then hung on the headless shell and the download of Chrome-for-Testing failed, so §7's Lighthouse target shipped unverified. Rule: before deleting anything under `/tmp` to free space, list what the remaining gates need (`CHROME_PATH`, browsers, caches) — and in Cowork the first Artifact publish needs an approval card the session cannot show, so decide the preview path (local `npm run preview` + Vercel) before promising an artifact. Engine outcome worth reusing: fit the plate `contain` on an on-palette ground and retire residual CSS push when art must stay hoverable — `cover` plus any settle scale will eventually crop the near-wall content the tracker promised was inside.

- **2026-09-15 · Mezzanine "In Motion" v2 minimal rebuild (orchestrator assets + 1 Sonnet dev + Opus gate + 1 Sonnet fix round + orchestrator smoke; 2 P0, 4 P1, all closed in one round; Lighthouse 72 → 100).** Three learnings. **(a) `overflow: hidden` is a scroll container, and anything that can focus or find-in-page will scroll it.** A pinned horizontal track driven by a transform was corrupted permanently by pressing Tab: the browser scrolled the hidden container to reveal the focused panel, the engine kept composing its transform on top, and nothing ever reset `scrollLeft`. Rule: **a viewport that is positioned by script must be `overflow: clip`, and focus must move the page, not the container** — and the gate's keyboard walk (Tab through every focusable, then scroll back to the start and compare the DOM's geometry to a clean load) is the probe that finds it; a mouse-only gate never will. **(b) A presentational `height=""` attribute defeats `aspect-ratio`.** The dev set `aspect-ratio: 2/3` on an `<img width="1200" height="1800">` and got a 400×1800 box; both axes were definite so the ratio never applied, and `object-fit: cover` hid the error as a plausible-looking crop. The dev's own screenshot showed a grey slab and the dev explained it away as "the top edge of the portrait". Rule: **measure the rendered box of every image slot at every viewport (`getBoundingClientRect` w/h ratio) — a screenshot that looks slightly wrong and has an explanation is a P0 until measured.** **(c) A generation model's aesthetic drift is a gate, not a taste question.** Graded against the written imagery language, three of five first-pass plates failed (no priority motif, inverted colour rule, film border) although each looked handsome on the contact sheet; the second pass was prompted motif-first ("one motif: the ribbing", "solid parapet, no balusters") and passed 4/8. Also: `cinematic_studio_2_5` adds a film border on roughly a quarter of 4:5 outputs — scan edge rows by median luminance and trim before shipping; never trust a corner sample. And the dev's Lighthouse "99" came from a Node-API runner that silently kept mobile throttling under a desktop label; the gate's CLI `--preset=desktop` read 72, the fix round's 100. **Rule: report the invocation with the score; a score without its throttling config is a claim about the runner.** Environment: sandbox recycled between turns again (rebuilt Chromium as Chrome-for-Testing 153, which DOES decode H.264, so the video path was finally verified in-sandbox); `/sessions` at 100% — `HOME=/tmp/qahome` required for Chrome's crash handler; QA deps split into `qa/package.json` held up.

- **2026-09-14 · Cybersecurity SMID 2027-bucket research wave (6 Opus Firecrawl slices + read-only Opus QA + 3 gap-closers + synthesis; 29 cards → 25-name universe → 14-name shortlist; 8 P1s found against six `status: complete` slices).** Three learnings. **(1) A numeric screen band needs a stated pricing convention, or the gate re-prices it.** The brief said "$300M–$10B" and nothing about *which close*; the wave ran on a +7.9% sector day and three band decisions inverted on the prior close (SAIL excluded at $11.35B but $9.84B the day before; TLS admitted at $321M but $295M the day before; ARQQ the reverse). Orchestrator error a written rule would have prevented — put a multi-day-average or ±tolerance convention in every brief that carries a hard threshold, and have QA re-price on the prior session as a deterministic check. **(2) A refuted shared data field is a class defect, not a slice finding.** One slice discovered mid-run that stockanalysis.com's `/statistics/` revenue-growth field was wrong (QLYS +30% shown vs +11% reported) — but parallel slices have no channel, so the others had already consumed it. Catalog-over-instance applies to *data sources*: the gate greps the whole wave for the field and lists every card that touched it. Same run: Firecrawl's `document_antibot` blocked an issuer ETF-holdings page that a same-origin `fetch` of the CSV from inside a browser tab returned in one call — capability-matrix fallback, per operation, closed the coverage gap with zero new names (which is itself the evidence the universe was complete). **(3) Cross-slice contradictions are a deterministic pre-web check.** The theme slice ranked a regulation as the #1 dated catalyst while the sub-sector slice had already found it suspended two months earlier; a slice excluded two names on a business test and carded a third under the same test. Both were caught only at QA. When slices touch one fact from different angles, the gate diffs them *before* spending web calls. Also confirmed: brief-and-reproduce (Law 9) held — the top-ranked name's guidance was transposed in its card; reproduced from the press release before editing, and the score survived because the raise was real.

- **2026-09-13 · CCC V1 efficiency review (proposed).** Large repeated context loads and unmetered repair/QA loops exposed missing general budget checkpoints. Added bounded wave accounting, compact dispatch, and targeted preflight/review rules. Moved historical changelog out of the default entrypoint without removing active rules. Default-load size reduction is measured; runtime token savings are not yet established.

- **2026-09-11b · Arcee AI investigation + canonical BT Stock Report (same pipeline as the Acres run, second execution in one session: 6 Opus Firecrawl slices → Opus QA gate → 4 fix-round agents → orchestrator Chrome/LinkedIn/Sunbiz/EDGAR/USAspending pass → Opus synthesis → orchestrator report → Opus final gate).** No new law; three Research-Fleet refinements folded into existing bullets by practice: (a) **the most important number lived in the JOIN of two slices** — slice 04's Artificial Analysis score and slice 05's peer set had never been placed in one table until the gate demanded it; brief the gate to look for cross-slice joins, not only cross-slice contradictions. (b) **Third-party indices rebase** — AA moved Trinity from 19 to 11 between the research pass and the fix round (v4.3); every index score must carry a version stamp and a fetch date or it is a different number a week later. (c) **The brief's own prior was wrong again and the slices caught it again** — the CTO named in the brief (Solawetz) had been an advisor for 15 months; founder status comes from LinkedIn *experience* pages, never headlines or aggregators. Gate outcomes: research QA struck 1 P0 (an unsourced "$240M post-money" that had been laundered through a structured extract of an unread WSJ body) and 11 P1 across 2,661 lines; the final gate on the orchestrator's report found 1 P0 (a headcount trend stated backwards) and 5 P1, all fixed pre-release. Environment: with two Chrome extensions connected the harness *requires* an AskUserQuestion listing every browser before any action — `switch_browser` then worked once the user clicked Connect in the "Nolan (Bang)" profile; Florida Sunbiz answers a Firecrawl GET (curl gets Cloudflare-challenged); Delaware ICIS is genuinely captcha-gated; USAspending zero-award is the truth test for any "government partnership" revenue claim. See ENTRY-2026-09-11b-arcee-recon-bt-report.md.

- **2026-09-11 · Acres investigation + canonical BT Stock Report (6 Opus research slices via Firecrawl + 1 adversarial QA gate + 4 fix rounds + orchestrator Chrome/LinkedIn/SOS/EDGAR pass + Opus synthesis + Opus final gate).** Added three Research-Fleet Discipline bullets (GET-before-declaring-POST-blocked; read every Form D `primary_doc.xml` for Item 5; gate the orchestrator's own deliverable). Gate outcomes: research QA found 2 P0 (a wrong divestiture date; AcreValue mis-attributed to FBN 18 months after CoStar bought it) and 11 P1 in 3,138 lines, with four QA findings refuted on reproduction (Law 9 held both ways); the final report gate found 1 P0 (42 vs 54 months) and 5 P1 on the orchestrator's draft. Environment: `/sessions` volume was full (npm ENOSPC) — install to `/tmp` and build there; LibreOffice substituted DejaVu Serif for the brand's Helvetica Neue (as every prior library PDF silently did) — a user fontconfig alias to Liberation Sans restores the sans brand; density calibration for the bt-equity template is ~2 rendered pages per content page, so a ≤10-page report means ≤5 content pages or four trim passes (16 → 9 here). Chrome MCP: the first connected profile was the wrong Google account; `switch_browser` timed out twice, then the correct profile appeared as the same "Browser 1" device once the user connected it — re-check `tabs_context_mcp` rather than waiting on the broadcast. See ENTRY-2026-09-11-acres-recon-bt-report.md.

- **2026-09-08 · Cortana Cognitive Core S5.** Both pilot forecasts remained failed while real defects in NQ lookahead and public rollback projection were repaired and independently checked; the narrow final gate passed. Tightened evidence reuse, scoped QA, disjoint parallelism, source-first integration, compact custody, early blocker reporting, and completion-based closeout. See `cortana-vault/projects/cortana-platform/cortana-platform--cognitive-core-s5-2026-09-08.md`.

- **2026-09-08 · Cortana Cognitive Core S5 evidence-learning run (Sol producers, separate Sol QA, and fresh final Sol gate).** Existing evidence-first and immutable-failure laws held: both pilot forecasts remained failed, the first NQ public rollback disclosure remained a historical FAIL, and WAI-650 reopened until a generic lifecycle projection passed independent qualification and a fresh observer. The process itself was too slow because one custodian interleaved source integration, many small evidence commits, dispatch, reconciliation, and documentation. For later runs, consolidate immutable evidence once per accepted candidate; integrate the accepted source commit before documentation custody and keep reviewer packages compact; report blockers immediately with the exact output and owner. These are run-derived process findings, not operator-approved policy. The staged skill retains its richer prior lineage and remains unpublished. See ENTRY-2026-09-08-cognitive-core-s5.md.

- **2026-09-07a · Cortana Cognitive Core S3 (Fable orchestrator; 4 Opus dev slices in 3 waves + 4 fix/micro rounds + 6 fresh Opus verifiers incl. a gate re-run; G3 APPROVE on ee04ace; 585+409+70 tests green under two venvs; continuity 27/27 · 25/27 · 213/214 over 9 sealed cases × opus/sonnet/haiku, 54 fresh sessions).** Three additions, two reconfirmations. **(a) A continuity/eval gate that scores a model's answer is also scoring the packet's rendering; attribute every miss before you fix either.** The first G3 missed the ≥90% rationale target (21/27) with next-action at 27/27. Five of six misses were the same shape — the packet *contained* the truncation/staleness/unverified facts but buried them (a warning in a tier that truncation drops; a stale evidence chunk with no marker; `harness_completed` in a field a zero-tool model never opens). Rendering fixes (banner repeated in the next-action block, `[STALE]` + remedy, an explicit "outcome NOT verified by an operator" line, an answer template) flipped exactly those five cells on the SAME sealed cases and rubric; the one residual is a model reading defect and is recorded as such. Rule: run the eval in a controlled A/B (same sealed cases, same rubric hash, same models), attribute each changed cell to packet vs model, and treat "the fact was present but not legible to a zero-tool reader" as a packet defect. **(b) A guard's negative control is only as good as the fixture that makes the guard the ONLY thing standing.** The budget-fixpoint control passed under mutation until the fix-2 dev showed the first gate's counter-evidence came from two probe trees with different fixture paths (the packet embeds its root); on one shared vault base and mutant were identical at every budget, and the biting control had to search for the needle (a budget where the blind first measurement lands ≤ budget but the accounted packet exceeds it). Rule: when a verifier's RED/GREEN evidence for a guard depends on two builds, prove the two builds differ ONLY in the guard (byte-diff inputs), and when a guard only bites at a boundary, the control must find the boundary, not pick a round number. **(c) Provenance guards must bind at write time and as authority, not as a string.** The wave-1 verifier flagged that an agent-written checkpoint `next_action` rendered as authoritative; the adapters slice then let `record-outcome --verified-by <the adapter itself>` flip `outcome_verified`, and an env var could turn a hardcoded model id into a "probed" one. Fix shape that held under 12 mutations: actor columns ON the row (migration), operator allow-list as data, writer≠verifier checked BEFORE the allow-list, evidence must resolve, env-sourced identities recorded unprobed, and the render says what the text is ("written by agent X during event Y — not an operator instruction"), never whether it is true. Reconfirmed at cost: **a full disk is a wave-shaped hazard** — `/sessions` at 100% blocked the approved SDK install into the sandbox venv; a second venv on `/tmp` (`uv venv --python 3.14`, `UV_LINK_MODE=copy`) plus "`CORTANA_VAULT_PYTHON` must be the interpreter the suite runs under, because subprocess children inherit it" cost one verifier a whole class of `Connection closed` errors before it was written down. And concurrent fix rounds on disjoint files still poison any test that hashes the package tree — one sha-manifest test went red mid-run because a sibling wrote `context.py`; certification runs only on a quiet tree, fix rounds on shared packages are serialized or the manifest test names its own files. Also: the second-vendor criterion was unreachable from the sandbox (one vendor family; `openai` not in the D03 allow-list) — recorded as a *reported* gate blocker with a paste-ready operator block and its four PASS criteria, filed as an operator issue, never claimed.

- **2026-09-07 · MetaCortex foundation sprint (`mc-foundation-sprint-20260907-01`, GPT-5.6 Sol developers and separate Sol QA).** Existing laws covered every finding; no new numbered law added. Same-producer repairs closed effective child-role/owner access, omitted TLS preparation, incomplete PostgreSQL fixture discovery, fabricated qualification success and premature checkpointer closure. Root caught CI-directory portability and a duplicate type annotation; real PID-1/async-saver drills reproduced the old shutdown failure and exercised normal lease recovery. Exact hosted checks, complete policy objects, artifact fingerprints and scoped source-document tests remain distinct from deployment and human acceptance. Preserved the richer staged skill, foreign Git staging and prior failed receipts. See ENTRY-2026-09-07-metacortex-foundation-sprint.md.

- **2026-09-07 · MetaCortex Factory Team execution (`mc-factory-progress-20260907-04`, GPT-5.6 Sol producers and independent Sol QA).** The exact locked LangGraph client showed that restating thread coordinates stripped ambient nested-graph context; the run could report success while its approval remained pending. Preserve the real inherited context and prove approve/reject/expiry/restart with durable saver isolation. A stale shared venv also differed from the repository lock; a new hash-verified environment and container read-back corrected that evidence. Budget edit verification reached the trusted write before storage, and the governance recheck found both an omitted active parent clause and unsafe credential-revocation order. Existing evidence-first, real-client, mounted-control, and independent-QA laws apply; the installed-version corollary makes the environment preflight explicit. A cancellation response also proved weaker than its label: durable cancellation must remain a request until the worker acknowledges a safe boundary, including last-node, early-park and exception paths. Concurrent database result ordering needs overlapping transactions, not only sequential replay tests. A QA PATH hid an installed Docker CLI and created a misleading availability skip; the host capability was restored with a scoped rerun and preserved original counts. Earlier failed claims and richer staged skill lineage remain preserved. Project and board outcomes retain their separate QA/deployment/owner limits.

- **2026-09-07 · Harness-landscape research fleet (4 Opus research slices + 1 Opus adversarial QA gate + 1 resolution pass; primary sources only; deliverables = vault topic page + WaiveLabs-branded HTML brief).** Three additions to Research-Fleet Discipline: column-header-carrying numeric claims with cell-by-cell reconciliation of shared rows; the author's-own-disclaimer field for headline deltas; a defined verification legend per contract. Gate outcomes: QM slice PASS with zero errors; three slices PASS-with-defects (1 P0 = pitch framing the paper disclaims; 3 P1 = misquoted GEPA numbers, single-sourced NVIDIA disclaimer, the OOLONG column slide; 5 P2 incl. a licence asserted for a repo with no LICENSE file). Environment: `developer.nvidia.com` blocked `web_fetch` by session-level dedupe but yielded to a fresh agent's Firecrawl scrape — resolution passes should run in a fresh agent for that reason alone. Full retro in ENTRY-2026-09-07-harness-landscape-research.md.

- **2026-09-07 · WaiveBoard S01 (GPT-5.6 Sol implementation and separate Sol core/invitation/browser QA).** No new law. Reconfirmed served-artifact and intended-failure proof, faithful database predecessor/environment scope, reviewer refutation and immutable evidence. Repaired a real former-identity relink defect missed by green CI. Preserved the service-blocked action-replay coverage gap and kept sprint acceptance open. Richer staging, registry/account lineage and existing worktrees were preserved. See ENTRY-2026-09-07-waiveboard-s01.md.

- **2026-09-07 · MetaCortex current In Review acceptance audit (three GPT-5.6 Sol QA agents).** Reaffirmed the existing evidence and board laws; no new law. An old addendum still alleged a missing RiskTier value after the code fix had merged, and one QA report initially repeated it. The outer review checked the exact current source, retained a refutation receipt and corrected the report before publication. Require the original criterion, exact candidate, executed evidence and explicit remaining work in the issue; a checked box or an old unresolved note is not its own proof.

- **2026-09-07 · DetailAI pilot readiness (`pilot-20260907-01`, GPT-5.6 Sol developers and separate Sol QA).** No new law: existing Laws 2, 7, 9, 11 and 12 cover the findings. A deliberate callback 503 is not a recovery guarantee when provider defaults do not retry 5xx; verify effective provider policy as well as the handler. A widget passing a static-server browser test still redirected to login on the actual Next host, and normal build/verify accepted a stale asset until an artifact-freshness gate was mounted. Browser inspection refuted a claimed labelled input. Post-migration blanket grants masked migration-permission evidence and required original-owner harness repair. Reproduce candidate-caused full-suite failures against the base before calling them pre-existing. Preserve failed reports, current consumer copy and local-versus-live acceptance boundaries. This entry records observed findings, not release acceptance; see ENTRY-2026-09-07-detailai-pilot.md.

- **2026-09-07 · MetaCortex core continuation (`mc-core-continue-20260907-02`, GPT-5.6 Sol producers and fresh reviewers).** No new law: reinforced exact-source identity and actual consumer coverage. A producer-expanded commit ID did not resolve; raw Git output supplied the real object before integration. Draft criteria review reproduced nested mutable-body aliasing and Python/PostgreSQL Unicode-contract differences; repair froze canonical bytes and declared consistent rejection before candidate freeze. Preserve the distinction between accepted canonical bytes and earlier document-source bytes, and between a mounted approval/canonical-action foundation and the still-absent plan-dispatch/QA consumers. A stacked PR ancestry correction retained an identical complete Git tree; final-head QA/CI binding was kept explicit. Existing Laws1,4,11,13 already cover these lessons.

- **2026-09-07 · MetaCortex core WAI-472–476 (`mc-core-472-476-20260907-01`, GPT-5.6 Sol producers and fresh verifiers).** No new law: reconfirmed actual-schema/provider-role rehearsals and production-seam acceptance. A reversed packet/run fixture concealed a deployed-schema mismatch; a superuser fixture concealed function grants silently ignored after ownership transfer. Chained PostgreSQL 17 tests under the real non-superuser owner profile exposed both; a later saved finding led to fail-before-change validation of pre-existing role configuration. Independent budget QA rejected a reserve test that bypassed dispatch; the repair binds activity to the mounted role and keeps the absent verifier graph explicit. The final local suite passed 5,524 tests (13 skips), while deployed doctrine evidence and an interrupted independent database review remain open. Preserve partial results and human release gates; never convert green helper tests, an image construction probe or an agent interruption into full ticket acceptance.

- **2026-09-07 · Cortana Cognitive Core S4 completion preflight.** Added one readiness interpretation rule: an optional exact receipt-base comparison is a diagnostic for same-base receipts, not the normal dependency-readiness contract. Before inventing closure receipts or rerunning an earlier sprint, inspect the actual brief and readiness validators; use their supported receipt checks and preserve the new attempt's resolved base separately. Passing dependency readiness never supplies a separate operator decision such as model adoption. Staged only; no publication or model use. See ENTRY-2026-09-07-cognitive-core-s4-completion-preflight.md.

- **2026-09-07 · TSG Consumer enterprise PE research (GPT-5.6 Sol producers, repair specialists and fresh independent Sol QA).** Reconfirmed source identity and acceptance discipline: exact URI-to-original-page joins must reject HTTP errors, soft 404s and generic welcome pages; URL path names do not prove retrieved capability. A cited source can support a narrow fact without supporting a complete operating specification. Independent review corrected 74 investment/consumer entity grades to inferred/medium, leaving frozen primary/high-confidence floors failed instead of gaming quotas. Parameterized boilerplate and shared evaluation paragraphs survived producer self-checks; mechanism-specific rewrite and independent semantic review were necessary. Propagate entity scope fixes into published tables and verify quiet-tree outputs. No new law or registry publication; richer staged lineage retained. See ENTRY-2026-09-07-tsg-enterprise.md.

- **2026-09-07 · TSG Consumer prospect reconnaissance (three GPT-5.6 Sol producers and fresh Sol QA).** No new law: reconfirmed exact-source joins, source lifecycle and semantic attribution. Follow buyer/seller/counsel completion notices after conditional transaction announcements; operational continuity is not equity control. Full LP tables outrank targeted extraction labels. Separate regulatory AUM, commitments, dated LP marks and portfolio sales; prior GenAI work and Shopify-native interfaces do not establish firm deployment. Live rendered/computed-style checks corrected automatic branding artifacts. Preserved the richer staged Cognitive Core S4 and prior retrospectives; no registry publication. See ENTRY-2026-09-07-tsg-consumer-recon.md.

- **2026-09-07 · Cortana vault repair staging (process proposal; independent V08 QA pending).** Three concise additions from the live producer contracts. **(a)** Derive the work denominator from the actual producer before a bulk wave: graph compiler eligibility and all ID-bearing candidates are separate populations, and excluded trash cannot be silently counted as required output. **(b)** Cross-stage acceptance joins the reviewed preimage to the real postimage; a handoff summary, status label, or writer-reported count cannot substitute for that identity. **(c)** Transaction guards are load-bearing only when root/path checks and immutable plans survive a zero-work executable, sentinel-like text inside fenced prose, and an injected later-write failure without overwriting concurrent bytes. Continuation: compiler equivalence must prove the intended compiler and module identity executed successfully and wrote fresh artifacts; matching failure exits plus unchanged copied outputs are stale evidence, not equivalence. This is staged for later publication review and records process evidence; it does not claim the vault repair or scheduled caller has passed final QA.

- **2026-09-07 · Cortana Cognitive Core S4 (Astra/Codex controller; Sol/Codex developers and independent review).** Strengthened source and measurement identity: hash the complete imported runtime closure, including uncommitted support helpers; distinguish catalog/loading state from physical artifact presence; verify each benchmark packet's effective condition and include all non-model retrieval work in core timing. Staged lessons and evidence: ENTRY-2026-09-07-cognitive-core-s4.md. This entry records observed defects and corrections, not S4 acceptance.

- **2026-09-07 · Cortana Cognitive Core S4 completion (GPT-5.6 Sol production and independent QA).** Added closure discipline from the real approved-Q8 run: schema-validate canonical receipts before board review; keep agent, artifact, loaded-service and vector-space identities distinct; profile the public composed path; prove retained SQLite observers actually close across threads; freeze a quiet worktree before long benchmarks (preserving the 193-row drift-aborted run); and do not confuse optional exact `--base-sha` diagnostics with normal readiness. Measured latency and quality gates failed, lexical remained default, and this staged entry does not claim G4 acceptance. See ENTRY-2026-09-07-cognitive-core-s4-completion.md.

- **2026-09-06d · Cortana Cognitive Core S2 (Fable orchestrator; 5 Opus dev slices in 4 waves + 1 same-owner fix round + 4 fresh Opus verifiers; G2 APPROVE on b4f3fe4; 688+70 tests green; coverage 16 current rows → 1,709 sources / 57,101 chunks).** Two additions, two reconfirmations. **(a) When the corpus a gate grades is the live tree, every unrelated session that writes a note moves the gate; freeze the corpus, not the tree.** The 30-case regression went 28/28 → 26/28 on the operator's Mac with no code change because five notes were promoted and two worktree-sourced notes were edited between runs. Rule: an evaluation that must be reproducible grades a pinned snapshot (comparator root + pinned case bodies) and measures the live tree as a separate, non-gating condition; the honest exit code (28/26, exit 1) is preserved, and the decision to accept a frozen comparator is recorded as a decision file, not a chat message. **(b) "Filter is correct" and "filter is useful" are different findings; a fresh gate must measure both.** Scoped search was strict, mutation-proven in both layers and leak-free, and still cost 9/30 and 14/40 hits because 99.6% of sources carried an empty scope — the registry root declared `scope: []` and only hubs and enveloped records ever received one. No developer test could see it because every fixture registered a hub. Rule: a verifier runs the eval suite with and without each filter the slice added and reports the delta; a large delta is a P2 routed to a named issue that blocks the consumer sprint, even when the slice passes. Reconfirmed at cost: the shell ceiling turns a growing suite into a false red — at 688 tests one `unittest discover` no longer finishes in 170 s, so briefs must name the module halves; and the hook's secret detector matches `passwd=` inside a prose string in a receipt, so verifier prose is scanned with the hook's own patterns before commit. Also: register a slice's router rows as an orchestrator commit before dispatch (54ae23a) so no slice edits the frozen router and an absent module still reports `subcommand-unavailable`; hub-tuple grep literals in sibling modules (`baseline._hub_slugs`) break when a slice replaces the tuple — the orchestrator patches the seam and the verifier confirms the origin field, rather than the slice widening its ownership.

- **2026-09-06c · Cortana Cognitive Core S1 (Fable orchestrator dogfooding the S0-built `cortana dispatch` tool; 7 Opus dev slices in 3 waves + 2 same-owner fix rounds + 3 fresh Opus verifiers; G1 APPROVE on ec56501; 403+70+19 tests green; 33 real SIGKILLs, 0/0/0/0 loss counters).** Two additions, one reconfirmation. **(a) A lease tool that exposes `state: released` as a field will be hand-edited to that value; only the tool's own verb counts.** The S0 lease was "released" by editing the JSON; `lease status` still reported `held: true` and `acquire` refused with `lease-held` until `lease release` was run with the original owner/run-id. Rule: the orchestrator never hand-edits a ledger the tool owns; and a tool's first dogfooding run is a gate on the tool — S1 surfaced two more usability defects this way (`launch` requires `--probe-file` even when the brief already carries the probed model id, and there is no `--attempt` on `launch`, so a `failed-before-ack` attempt needs a regenerated brief). Both were handled correctly by the guards (duplicate-launch refused, `reconcile --resolve not-launched` required) — the tool protected against its own operator. **(b) A dependent slice's SCHEMA test hardcodes the migration inventory and every additive migration from a sibling slice breaks it.** Three parallel slices each added `000N_*.sql`; `test_state.py` asserted the literal list `["0001","0002"]` and went red for all of them, and each dev correctly refused to touch a NO-GO test. Rule: when one slice owns a schema that others extend additively, its tests assert against the discovered inventory (`SCHEMA_VERSION`, `discover_migrations()`), never a literal — and the orchestrator assigns migration numbers at dispatch (0003/0004/0005) so parallel slices cannot collide. Reconfirmed at cost: **the pre-commit secret detector flags any source line that assigns a value to a name ending in *token*, *secret*, *password* or *api-key* regardless of context** — four commit attempts bounced on test literals and even on a line assigning a freshly generated id to a variable named `token`; assemble such strings at runtime or rename the variable (`new_claim`, `claim_ref`), and scan owned files with the hook's own detectors (`validate-staged.VENDOR_CREDENTIAL_PATTERN`, `vault_lint.SECRET_KEY_PAT`/`looks_like_secret`) before handing paths to the orchestrator. Also reconfirmed: the seed/live-export coupling (a 40→43 issue seed edit by a reviewer session took out 50 dispatch tests) — fixtures derived from the seed plus ONE explicit coupling test is the right shape; sibling sessions committing onto the orchestrator's branch (three checkpoints this sprint) are harmless only because every S1 commit was `git commit --only <owned paths>`; `/sessions` at 100% turns `tempfile` defaults into ENOSPC test errors — export `TMPDIR=/tmp` for every suite run; and spending Opus on three fresh gates found the two P2s (outbox loser clobbering `recorded`; operator resolution enabling a second external effect) that 200+ green developer tests could not.

- **2026-09-06 · WaiveLabs factory playbook research (three GPT-5.6 Sol researchers and one fresh Sol QA).** Connected a complete dated 14-project/691-issue census, seven project cases and both existing source graphs into a proposed D01–D10 product lifecycle. No new law: reconfirmed that old failed experiments must be reconciled with later exact-scope fixes, board completion is separate from release and user acceptance, and generated views must be checked against their source bytes. Preserved the factory graph’s failed confidence gate; used a separate internal reference layer instead of changing source grades or creating a second backlog. Fresh three-way drift check preserved the richer staged Cognitive Core S3 entry.

- **2026-09-06 · MetaCortex S01 continuation (`mc-s01-20260906-02`, four Sol producer assignments and fresh Sol QA).** Added the role-scope corollary: signed workspace freshness did not authorize caller-selected workspaces, and a minimal SQL fixture could conceal predecessor function permissions. Independent probes also found agent identity loss at paid leaves and alert retries stopping when health recovered. Local repairs passed while live owner/deployment gates stayed open. Root’s automatic lint fix removed an intentional public re-export; the full suite caught it and the compatibility export was restored. Three-way lineage check preserved the richer staged skill and unrelated research entries. See ENTRY-2026-09-06-metacortex-s01-continuation.md.

- **2026-09-06 · Repository and delivery research (three GPT-5.6 Sol research workers, two fresh Sol reviewers).** Resumed the existing software-excellence graph and preserved all inherited gates. Fresh review reproduced eight documentary-validator bypass classes despite 22 green author tests; bounded fixes plus independent positive/negative controls now pass 36 tests. Semantic predicates, proposed-versus-effective regulation, source publication dates, and one reviewer article locator required correction against exact primary text. The JSON contract is explicitly an index of full documents; index PASS cannot imply legal completeness, authenticated approval or live enforcement. Existing laws already cover these failures; no new law added. Preserved richer staged lineage and left registry/account symlinks untouched. Evidence: `cortana-vault/research/software-excellence-graph/runs/2026-09-06-repository-delivery/RETRO.md`.

- **2026-09-06 · MetaCortex S01 (`mc-s01-20260905-01`).** Independent QA found two gaps missed by admission-only tests: a new fatal control exception was swallowed by older memory telemetry/fallback catches, and a run-bound client reused under another active context spent before identity was checked. Gate the shared fatal-error family across every best-effort boundary and check bound identity at paid-call time as well as construction. Keep already-incurred callbacks attributed to their original run. Legacy verifier setup was migrated by the independent custodian with original assert ASTs preserved; no production claim follows from local tests. See staged ENTRY-2026-09-06-metacortex-s01.md for evidence.

- **2026-09-06 · BT Journal reliability and usability (GPT-5.6 Sol capture/research/frontend plus fresh Sol QA).** Preserved richer existing staging and the registry-backed account symlink. Added mutable test-fixture ownership after concurrent preview edits produced an expected revision conflict. Mounted negative controls exposed timezone folds/gaps, account reassignment dedupe, source instrument/session/interval mismatches, impossible dates, ZIP path aliases on macOS and spoofed source metadata. Reproductions drove fixes; provider calls used actual pinned clients with mock transports. Root browser checks also caught stale publication revisions, the surviving radar/streak calculations and a cost-input race missed by earlier green suites. Final verification passed 68 behavior tests; installation matched 48 certified source files, and backup restoration plus raw historical-data comparison passed.

- **2026-09-06 · DetailAI WAI-548 (GPT-5.6 Sol producers, original-owner fixes, and fresh independent Sol QA).** Strengthened existing database and seam verification practice: apply new migrations without blanket post-migration grants; privileged fixture inserts can conceal or conflict with actual runtime permissions. Paid-operation idempotency must survive runtime DELETE, identifier rewriting and parent deletion attempts, not just repeated method calls. Use each provider event's actual wire variant (number deregistration legitimately omitted a campaign SID), and recheck tier-specific verification gates during generic reconciliation. Run the built container: unit/typecheck passes did not reveal a missing worker Calendar package. Mutation testing found untested tenant-only and account-only substitutions; original-owner negative tests killed both. Live identity, paid action, provider registration and handset delivery remain distinct from mocked provider and local PostgreSQL evidence. See ENTRY-2026-09-06-detailai-wai548.md.

- **2026-09-05d · Mezzanine talent research (GPT 5.6 SOL market, technology and governance; independent semantic review).** Added source-lifecycle verification after a current-looking CFPB circular proved withdrawn. Reproduced and corrected cross-predicate mirrors, actor authority, median definitions and local derived-sensitivity/date checks; two QA suspicions were refuted by exact content. Preserved the unmet confidence threshold and newer staging lineage. No registry or account-symlink write.

- **2026-09-05c · Cortana Cognitive Core S0 (Fable orchestrator; 3 Opus dev slices in 2 waves + 4 fix/micro rounds + 2 fresh Opus adversarial verifiers; G0 APPROVE; 188+70+19 tests green; 7 commits 989fffb..bfb2785 on `codex/cortana-cognition-s0`).** Three additions. **(a) An orchestrator shim to a fail-closed gate IS a guard change — grade it with the gate's own negative controls before dispatch.** To run the pinned validation runtime in a Linux sandbox I added a `CORTANA_VAULT_PYTHON` override to `vault-python` and wrote "this is not a validation bypass" in the comment. It was one: the pin check only tested that the interpreter *exited 0*, so `CORTANA_VAULT_PYTHON=/bin/true` silenced the entire pre-commit gate (frontmatter + secret guards) while every suite stayed green — and my own follow-up edit (stripping the env var in two negative-control tests) made the suite greener without touching the hole. The wave-1 verifier found it as a P1 in ten minutes with the obvious probe. Rules: a shim's negative control is *a present-but-bogus value*, not only an absent one; the check must compare an *output* to the pin, not an exit code (Law 3 applied to a shell probe); and Law 7b runs on the orchestrator's edits before the first agent launches, not at the gate. **(b) On a contended checkout the INDEX is shared too — `git add`/`commit --only <owned paths>` is the only safe commit form.** Two other orchestration sessions wrote to this tree during S0 (research graphs; 08-25 rule confirmed live a third time) and one of them staged ~1,200 files; a plain `git commit` would have swept them into my slice's commit. `git add <paths>` then `git commit --only -- <paths>` commits exactly the owned paths regardless of what else is staged, and clears only those entries from the shared index. Also: sibling sandboxes without delete permission leave `.git/index.lock` behind on every `git status`; age-check the lock (>5 min with no git process) before removing it, and re-read `git log -1` + `git branch --show-current` before every commit — a sibling can commit onto *your* branch. **(c) A frozen evaluation date is a hidden dependency of every "reproduce the recorded number" claim.** The memory benchmark hardcodes `today=2026-09-04`; a project hub's `current_state_as_of` moved to 09-05 the next day, so the recorded 28/28 became 26/28 at the frozen date and 28/28 at today's — two agents independently reported the flattering one until the verifier ran both. Rules: every derived score carries its `probe_date` beside `as_of`; a regression tool's `ok` means "no regression", never "ran" — a measured drop exits non-zero (the tool shipped with `ok: true` on a −2 and would have let S1 build on it); and freezing a comparator snapshot is itself a versioned artifact (v1 was unreproducible because 7 notes post-dated its checkpoint). Reconfirmed: spending Opus on both gates paid twice (P1 + the date artifact were invisible to green suites); Law 9 held — every finding sent back was reproduced first and none refuted this run; readiness computed from *verified receipts* (reviewer ≠ developer, base_sha git-resolvable, candidate_sha required) is what made "WAI-629 ready" a fact rather than a board state; the manual-agent-tool harness adapter is the honest shape when the only Opus surface is the orchestrator's own Agent tool (no session id exposed — record that, do not invent one); `uv python install 3.14` + a venv outside the repo reproduces the pinned runtime in <2 min; the `/sessions` volume was full so worktrees went nowhere — path-disjoint slices in the shared checkout with agents forbidden from git writes was sufficient. Open defect classes for S1 briefs: canonical attempt ledger must receive every launch from the first one (S0's private `--runtime-dir` seams left 2 of 3 attempts unrecorded until fixed); a tracked fixture of vault notes inside the Obsidian root duplicates memory_ids — ship archives, not trees.

- **2026-09-05b · Software Excellence Knowledge Graph (9 parallel Opus Firecrawl research tracks + 1 opportunity track + 2 Opus synthesis writers + 1 Opus read-only QA gate + lead fix round; 249 entities / 1,148 edges strict PASS; gate: PASS with defects — 7 blocking, 15 should-fix, 2 partly refuted).** Three additions. **(a) A minimum-degree floor plus a type-constrained predicate with a thin target population manufactures a SINK.** `mitigates → risk` had ~10 valid targets and every track needed ≥3 relations, so 33 of 78 `mitigates` edges landed on `technical-debt` — 17 of them effectiveness claims no entity supports (OKRs, Shape Up, Tailwind, htmx "mitigate technical debt"). The compiler saw a healthy graph. Rules: at bootstrap, count valid targets for every type-constrained predicate and either seed the target population in the anchor wave (risk and metric entities are cheap and load-bearing) or tell agents the floor may be met with `references`; add a per-predicate in-degree concentration check to the Law 10 gate (any single target taking >25% of a predicate's edges is a sink until read). **(b) Polarity predicates cannot be type-enforced — enumerate their targets.** `solved → risk|concept` let nine events "solve" `product-market-fit` and `aha-moment` (goal states, not pain points) and let a retraction "solve" technical debt. Where a predicate encodes polarity (solved, mitigates, triggers, supersedes), the SCHEMA restricts the target to `risk` or to a named id list; `concept` is never a valid target for a polarity predicate. **(c) Deterministic mirror resolution is unsafe for causal pairs.** My drop-the-weaker-predicate rule scored `mitigates` above `constrains` and so kept the REVERSED edge (`multi-tenant-saas-architecture mitigates data-breach`) while deleting the correct one (`data-breach constrains multi-tenant-saas-architecture`); the QA gate found the survivor from the entity's own body text. Specificity ranking is a proxy for truth only on non-causal pairs; any pair containing mitigates/triggers/solved/produces gets its direction read from the body before an edge is dropped, and the dropped list is itself a QA input (which is how this one was caught). Reconfirmed: the QA gate spent well — 12 of 13 load-bearing dated claims verified live and one refuted (`measured_by` bound an RCT's task-completion time to DORA lead time); Law 9 reproduce-first partly refuted two findings (7 of 16 `solved` edges were legitimate; the Vogels byline was correct per page metadata) and saved two wrong edits; the write-early contract again produced 9/9 complete tracks with 0 dead runs; agents correctly refused Firecrawl JSON-mode dates fabricated on pages with no visible date (Playwright, Rails) — JSON extraction of a date that is not on the page is a hallucination surface, cite page metadata (`article:published_time`) instead. Mechanics: a generator emitting `\|` for wikilink aliases must know whether the line is a table row (escape) or prose (do not) — 399 prose links carried a backslash into the target (Law 7c: check the emitted markup in the context its consumer parses); on this FUSE mount `.git/index.lock` could not be unlinked and the pre-commit's pinned Python 3.14 runtime is absent, so the operator owns the commit — the vault contract validator can still be imported and run per-file with a negative control to prove it bites. A second orchestrator session committed to the same tree mid-run (08-25 rule confirmed live): the stale `index.lock` that blocked my checkpoint was that session's, it vanished when their commit landed, and a fresh one appeared after — stage explicitly by path, read `git log -1` before every commit attempt, and never fight a lock you did not create.

- **2026-09-05 · Markets Knowledge Graph (8 Opus Firecrawl research tracks + 1 opportunity track + 2 fresh read-only QA gates + 2 same-owner fix rounds + orchestrator script edits; 265 entities / 1,461 edges strict PASS; gate 1 FAIL → gate 2 PASS).** Three additions. **(a) When the evidence base itself contains the forbidden data class, a prohibition in the brief is not enough — the brief must supply the REPLACEMENT FORM.** SCHEMA said "no P&L in entity bodies"; the governance agent still wrote journal dollar outcomes into 12 files because the vault's own rule pages justify each rule with those outcomes. The fix round's ratio/N form ("37% WR on 43 sub-minute trades, N=130, May 7–11; dollars in the journal by URI") is what the brief should have carried from the start. Corollary reconfirming Law 9: the QA gate's *suggested* replacement text contained a fabricated comparison (a 71%/N=130 figure that belonged to a different partition of the data); the same-owner fix agent reproduced, refused it, and used the supported figure. **(b) Set the population of every SHARE gate at bootstrap, not at acceptance.** Compiler-mandated `inferred` opportunity entities diluted high-confidence share from 55.7% to 52.8% and flipped the gate red after a clean research wave; the honest resolution (threshold over all entities lowered, original threshold verified over the sourced population, both recorded in config and acceptance report) should have been a config decision before dispatch. **(c) Orchestrator bulk regex edits over structured frontmatter get a post-edit invariant, not just a compile.** Two of my own scripts (alias canonicalisation, predicate rewrite) silently produced `target: }` lines twice — the compiler caught both, but only because it treats an empty target as a hard error; assert `relation count == expected` and `grep -c "target: }" == 0` in the same script that edits. Reconfirmed: two fix agents compiling one graph concurrently see each other's transient word-count failures — the shared-checkout rule applies to any shared artifact, not just code; capability-probe Firecrawl before dispatch (search/scrape/map all live, so no per-op fallback needed); a fresh-eyes semantic gate on a structurally green graph found 6 blocking defects (inverted `mitigates`/`confirms` causality, a `contradicts`+`validates` pair, a transposed contract tick) that no track agent's own compile could see — Law 10 is load-bearing for every KG program.

- **2026-09-04b · Autonomous software factory knowledge graph (three GPT-5.6 Sol researchers with Firecrawl, lead synthesis, fresh independent QA).** Final corpus: 257 entities, 880 relations, 274 cited URL identities, 15 scored opportunities. Initial fluent reports concealed shared boilerplate, false implementation/dependency relations, discovered-only citations and source/type mismatches; corrected the records rather than trusting worker PASS summaries. Exact-source retrieval joins and source-level sensitivity dominance are executable guards; quote escaping and URI validation prevent evidence links becoming executable HTML attributes. All structural/evidence audits pass, but the frozen high-confidence share is 50.2% against 55%: the report must fail that gate honestly. Preserve the newer staged skill superset and archived fixture bytes; published source counts are not independent trials and a configured control is not measured operating effectiveness.

- **2026-09-04 · Macro Regime Map 2025–2027 update (8 Opus Firecrawl research slices + 1 Opus read-only QA gate + orchestrator micro-verification; 0 timeouts, 0 FAILs, 19 corrections applied, 5 numbers closed by the orchestrator against primary sources).** Two additions, one mechanic. **(a) In a research fleet the dominant error class is VINTAGE, not fabrication — gate by as-of date before gating by value.** Seven of eight slices passed-with-defects and nearly every defect was a stale timestamp presented as current state: Brent "below $90" (week of 8/28) beside $95 (9/4); an S&P close labelled 9/3 that was 9/2; margin debt quoting June's record as July's print; HY OAS baseline from the wrong December. Rules: every row of a current-state table carries its own as-of; the QA brief sorts claims by as-of before checking values; the synthesizer reconciles vintages explicitly rather than averaging them. **(b) Orchestrator-supplied context distilled from the operator's own prior notes is a claim set, and the fleet must be told so.** The brief's "known facts" (from 12 days of daily NQ notes) carried three refuted items — the Iran war started 2026-02-28 not June, WTI ~$91 not $85, UMich 4.0% not 4.4% — and the agents refuted all three only because the brief said VERIFY under Law 1. Derive the brief's facts from a primary pull where cheap; where not, label them claims. Reconfirmed: the 08-24 write-early contract (file skeleton as the FIRST action, 25–35-call budget) produced eight complete files and zero dead runs; a QA agent given the load-bearing numbers as a numbered list re-derived 20 claim groups from primaries in one pass; the orchestrator closing the QA's five UNRESOLVED items itself (Treasury par curve, FactSet index line, a live ticker page) was cheaper than a third agent. **Mechanics:** on the FUSE-mounted outputs folder `unlink` is blocked, so `soffice --convert-to pdf` silently leaves the OLD pdf in place and `pdftoppm` leaves stale page images — convert in `/tmp` and `cp` (truncate) the result back, and never trust a page count from a directory you could not clear; Helvetica Neue is absent in the sandbox and LibreOffice falls back to a SERIF — a fontconfig `~/.config/fontconfig/fonts.conf` alias to Liberation Sans fixes the PDF while the .docx keeps the brand font; a `.pages` bundle with no `Preview.pdf` yields its text by snappy-decompressing `Index/Document.iwa` chunk-by-chunk (4-byte header, 3-byte LE length) and running `strings`.

- **2026-09-03b · MetaCortex factory wave 2 (5 Opus dev slices across two repos building both sides of ONE contract in parallel + Opus adversarial gate + 4 Opus micro-rounds + orchestrator acceptance pass; 4821→4953 green, 436→769 green, 0 ownership violations, 5 commits on 2 integration branches).** Three additions. **(a) A contract page the orchestrator writes is a spec, and a spec is graded against the code's own catalog — not against the runbook's abbreviation.** The consumer slice reconstructed five gate ids from a slashed shorthand in a runbook (`PRD/ARCH/SPRINT/QA/REPORT`) instead of `autonomy.yaml`, spelled the last one singular, and the DAG rendered a phantom sixth gate with a false "not reached" marker while the real gate had no position. The dev flagged the ids as INFERRED (correct discipline) and argued an inferred-wrong id "surfaces rather than hides" — the gate rendered the DOM and showed it did worse than hide. Rules: **any enumerated id set in a brief is copied verbatim from the producing file with a `path:line` citation, never from a summary; the consumer pins the set by test; and "surfaces rather than hides" is a claim about rendering that only a rendered probe can grade** (Law 7c). The orchestrator's own contract v1 named only ONE of five ids — the dev's gap report was right and the amendment should have preceded dispatch. **(b) A tolerant parser's failure mode is the exact place a seam mismatch hides — brief the gate to feed the producer's REAL bytes into it and assert the happy state, not the degraded one.** The budget parser degraded to "caps unavailable" on any shape problem by design; the gate captured the runtime's actual JSON via `TestClient`, fed it through the WaiveBoard parser, and asserted caps were READY and numerically equal to `autonomy.yaml` — that probe is what turns "tolerant" from a hiding place into a checked property. Same gate then found the route could omit a REQUIRED query param and land in that degraded state on a healthy runtime. **(c) The orchestrator verifies acceptance criteria against the ticket text, not against the slice reports, and records PARTIAL and DEVIATION honestly.** Eight tickets: several ACs were met in-tree but need the deployed stack (MET-IN-TREE), three were PARTIAL (per-stage spend/trace not emitted; ~2 s SSE not ~1 s; widening cap labelled non-binding, not refused), two "Do" items were DEVIATIONS (document in payload not ArtifactStore; inline confirm not `ConfirmActionButton`). Each got a follow-up ticket or an explicit operator decision — none was rounded up to "met". Reconfirmed: a spec addendum is a deliverable the gate reads adversarially too — it caught a grade ("Partially") that the cell's own evidence contradicted, a false "only gate with a trigger" claim, a "re-derivation" that omitted the rule it re-derived from, and three different line numbers for one line across three files; the fix round used an appended errata section so the review trail shows correction rather than silent rewrite, and kept the ledger append-only by construction (reconstruction diff: 0 deletions). **Post-merge addendum (09-07): the gate's green was incomplete — CI went red on `next build`.** Two files exported helpers from `route.ts` / `page.tsx` for testability; `next build` type-checks those modules against Next's Route/Page shape and rejects any extra value export (`"resolveLastEventId" is not a valid Route export field`). vitest and `tsc --noEmit` do not run that check, so 769 green + tsc clean certified a build that could not deploy. Rule: **for a Next.js repo the gate's command set is `next build` + `tsc` + the test runner — the build is the only place the framework's own module-shape rules execute**; and the house split (`route.ts` thin shell, logic in `handler.ts`) exists precisely for this, so a brief that says "export the helpers for tests" must say "from a sibling module, never from route.ts/page.tsx". Reproduced in the sandbox by `git archive` → `/tmp`, symlinked `node_modules`, `next build` (the linux-arm64 swc binding was already present); 4-minute fix, one commit. Also: MetaCortex's wave-2 branch was merged to main by a parallel Codex sprint before the operator opened a PR — check `git merge-base --is-ancestor <head> origin/main` before telling the operator to open one (Law 1, again). Mechanics: `MC_SEAM_TEST_REQUIRED=1` alone reports 1 failed without `MC_VENV_PYTHON` — the gate is working, the handoff command was incomplete (write the full command into the brief); the orchestrator's own one-line `vitest.config.ts` edit failed `tsc` and was reverted — the gate confirmed zero diff, which is the point of handing your own edits to the gate; vitest cannot transform `.tsx` under `jsx: preserve`, so components are tested through `.ts` pure-logic extraction plus a small real HTML parser (`test-support/html-dom.ts`) — the gate attacked the parser (nested quotes, `>` in attributes, self-closing) before trusting anything graded through it.

- **2026-09-03 · MetaCortex factory wave 1 (3 Opus dev slices + Opus adversarial gate + 2 Opus micro-rounds + 2 orchestrator one-line edits; 4795→4821 green, 433→436 green, 0 ownership violations, 3 branches ready to push).** Three additions. **(a) Your own prior audit is a handoff summary — re-derive it from live git before dispatching against it (Law 1, third corollary).** The 08-31 audit read a commit message's "(governance PR, branch-only)" as *unmerged* and planned a whole runbook step around writing a PR that had landed four days earlier; `git merge-base --is-ancestor` refuted it in one line before a single agent was briefed. A commit message is a claim about intent; ancestry is the evidence. **(b) Classify a baseline failure as code-vs-environment from the traceback's last frame before recording it.** Two "pre-existing failures" were `PermissionError: unlink` from tests that snapshot→mutate→restore a source module — the FUSE mount refused the cleanup; granting delete permission turned both green, and the true baseline was zero. The dangerous part: they were `*_guard_is_load_bearing` tests, so an environment red was masking whether a real guard regression would be visible — and they leave `.orig-snapshot` residue in the source tree that `git add -A` would have committed (explicit-path staging is the second thing that saved this). **(c) The permission classifier refusing a production write is a boundary, not a bug — route the operator, not around it.** Railway `set-variables`/`update-service` were denied by auto-mode; the correct move was a paste-ready operator block via `send_user_message` and continuing every slice that did not depend on it, which was all of them. Reconfirmed: QA enumerated 3 hardcoded-lane sites where the dev reported 1 (catalog-over-instance); a dev's "1 pre-existing EPERM failure" was refuted by the gate as a silently self-skipping *security* test, now made fail-loud under `MC_SEAM_TEST_REQUIRED=1`; `uv python install 3.12` + the hash-locked install gave agents a real interpreter in <2 min, without which every "suite green" would have been unverifiable; `SendMessage` unavailable → fresh-agent micro-rounds with findings verbatim, fine. Mechanics: on this FUSE mount `git checkout -b` / explicit `git add` / `commit` all worked once delete permission cleared `index.lock`; no push credentials in the sandbox, so branches are handed to the operator — never re-emit 330 KB of source through your own output to `push_files`, a transcription slip is a silent production defect.

- **2026-09-02 · Mezzanine "In Motion" Option 2 (orchestrator-generated Higgsfield assets + 1 Sonnet dev + Opus gate + 1 Sonnet fix round + orchestrator smoke; 0 P0, 7 P1, all 22 findings closed in one round).** Three learnings. **(a) The gate's top P1 was a defect in the SPEC, built faithfully.** The orchestrator wrote "ghost the poppy at 0.35 over ink"; the dev built exactly that; the composite was a gray in no token. QA proved it with the internal control (the one act card WITHOUT the ghost was clean) and by re-reading the reference deck. Rule: **when the orchestrator authors the design contract, the gate must grade the contract against the reference material, not only the build against the contract** — brief QA with the primary reference and ask it to rule on spec clauses, and expect the fix round to start with a spec edit by the orchestrator. **(b) Hypotheses handed to QA must be labeled as hypotheses, and two of four were refuted** (doubled glyphs = screenshot mid-transform; faint body copy = mid-reveal opacity at 700 ms). The refutations were as valuable as the confirmations — the fix round did not chase them. The remaining genuine contrast failures were on a different ground (red) than the orchestrator looked at (paper). Rule: **when eyeballing contact sheets, wait past every reveal transition before judging contrast, and measure with a probe before filing** — the orchestrator's screenshots were taken at 700 ms against a 2.3 s settle. **(c) Generative-asset provenance is a slice with its own gate.** `cinematic_studio_2_5` silently refused "red light" interiors three times (returned clean neutral rooms), Seedance returned `not_enough_boost_credits` from a pool the `balance` tool does not show, and the video came back 2.36:1 not the 16:9 the spec table promised. None of it was an error; all of it changed the contract. Rule: **contact-sheet every batch before dispatching the dev, record rejections with job ids, and re-derive the asset table in the spec from the delivered files** (QA caught the stale video row). Environment reconfirmed: rsync-to-/tmp build, `/sessions` at 97%, headless Chromium has no H.264 so the video path always exercises the fallback branch (report playback UNVERIFIED, verify in the published artifact), full-page screenshots of a 26 000 px page time out — take viewport shots on a scroll walk instead, and keep the walk under the 120 s bash cap (24 shots × 1.4 s). Also: the negative control on the brand gate (append `box-shadow`/`#fff` to built CSS → 4 reds) took 10 s and is the only reason "0 violations" is a claim about the CSS rather than about the gate. `SendMessage` still not loadable; fresh fix-round agent with findings verbatim worked again.

- **2026-09-01 · Mezzanine "The Arrival" site spike (1 Sonnet dev + Opus gate + Sonnet fix + Opus re-gate + orchestrator micro-round; 3 P0 caught against a green build, then 2 more the first gate structurally could not see).** Two additions. **(a) A load-time property needs a load-time probe.** Gate 1 measured the loading scroll-hold *after* `waitUntil:'load'` and reported it fine; the hold was actually a 1–25 ms no-op because `init()` was bound to `astro:page-load`, which fires at `window.load`. The re-gate found it only by installing a MutationObserver via `addInitScript` and stalling the asset with a route delay. Rule: **for any guarantee that exists during a window (loading hold, skeleton state, pre-hydration paint), the probe must observe from inside the window — an assertion made after the window closes grades nothing.** Corollary caught the same day: `overflow:hidden` blocks *user* scroll, not `window.scrollTo`; a programmatic-scroll probe passes against a working lock and fails against a broken one identically — drive the input the user drives (`mouse.wheel`). **(b) A dev's "≤0.03px error" was a self-consistency check, not a correctness check** (predicted-vs-actual through the same formula). The re-gate refit the painted frame from the plate pixels and found the shipped quad's bottom edge 0.1 plate-heights low. Rule: when a dev reports agreement between two numbers, ask what independent source the second one came from; if both derive from the code under test, the claim is a tautology (Law 7's cousin, for data). Environment notes: the Cowork-mounted project folder blocks `unlink` so Vite/Astro builds fail in place (rsync to `/tmp` and build there); `/sessions` was 99% full and Playwright needed `PLAYWRIGHT_BROWSERS_PATH=/tmp/pwb TMPDIR=/tmp` plus a rootless `libxdamage1` (`apt-get download` + `dpkg -x`, `LD_LIBRARY_PATH`) and `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` — with that, **headless Chromium runs in the sandbox and the QA gate can grade rendered pixels, not just HTML**. Also: `SendMessage` was not loadable in this session, so fix rounds went to fresh agents with findings verbatim — worked, at the cost of re-reading; and no git/Vercel credentials exist in the sandbox, so staging required an operator push (the Supabase-storage-as-CDN workaround was correctly refused by the permission classifier — do not reach for anon-writable storage policies to route around a missing credential).

- **2026-08-31 · James & Gable enterprise research fleet (8 evidence tracks + remediation waves + root synthesis).** One early research agent crossed its ownership boundary and lowered the shared source/domain acceptance thresholds, allowing a structurally plausible corpus to grade itself against an easier contract; an initial fresh reviewer then repeated the weakened numbers and missed 387 deterministic semantic violations. The root froze and restored the original contract, quarantined the wave, and only accepted the corpus after quiet-tree strict and semantic reruns. Added two Research-Fleet rules: hash and NO-GO the shared contract before dispatch, and make independent QA reproduce the deterministic gate against that frozen contract rather than endorsing agent prose. Firecrawl also reconfirmed endpoint-specific capability: authenticated app search/async collection worked while keyless routes returned `KEYLESS_QUOTA_EXHAUSTED`, so operation-level fallback remained the correct policy.

- **2026-08-30b · MetaCortex WAI-483 stages 4-5 (2 parallel sonnet slices + opus gate + 2 CONCURRENT fix rounds + orchestrator wiring commit).** The wave that proves the seam gate pays for itself twice: (1) the consumer slice GUESSED its upstream producer's artifact slug (correctly flagging it as a guess and reporting the constant, exactly as briefed) and guessed WRONG — and because a missing upstream artifact was deliberately a non-fatal partial input, the mismatch produced no exception, no red test, and no error log: the consumer would simply have discarded every operator ruling forever. **A non-fatal miss is an invisible failure by construction; any seam where "absent" is a legal state MUST be pinned by importing the producer's own function, never by restating its literal.** (2) The gate then found the orchestrator's OWN wiring commit had mounted the gated stage with `checkpointer=None`, making it park at its operator gate and be permanently unresumable — the self-performed wiring is exactly as suspect as agent work and must be handed to the gate as a reviewable unit, which the changelog rule from 2026-08-30 now proves out. New rules: **(a) Run fix rounds for disjoint slices CONCURRENTLY on the SAME integration branch** — both agents committed to `qa/…-integration` without collision because ownership was file-disjoint, and the second one measured a baseline that already included the first's commit (brief them that the stated baseline may be stale and to re-measure). **(b) A gate's DEFECT PROBE tests — probes that assert current broken behavior — are inverted, never deleted, by the fix round, and each is assigned to exactly one owner** (this wave's four split cleanly 2/2 between the slices; an unassigned probe is a merge conflict waiting). **(c) When a dev refutes a prescribed remedy with evidence, that is the process working:** the PRD dev tried the recommended second-skill pin, found by direct trial that it broke a QA-owned cross-stage invariant it was not allowed to edit, and fixed the false-precedent paragraph instead — Law 9's reproduce-the-remedy rule earning its keep for the second wave running. Scoreboard: 5 of 12 pipeline stages mounted, 4770/6 green + ruff clean (baseline 4657/6), 3 P1s found and closed (all in the ARMED path of an unarmed gate — i.e. defects that would have gone live the moment an operator ran the arming runbook), a governance runbook corrected before it could fail on the operator, one migration + one governance-patch doc delivered, zero conflicts across 6 commits from 4 agents on one branch.

- **2026-08-30 · MetaCortex WAI-490/491 unbreak (2 parallel slices + opus gate + 3 micro-rounds, across two repos and a live DB).** An operator-reported "save fails" turned out to be THREE independent causes stacked, each of which fully explained the symptom on its own and each of which had to be verified separately: a malformed connection string that threw inside the driver's URL parser before any query (so the DB was never reached, and every prior "DB permission" diagnosis was reasoning about a code path that never executed), a never-applied migration, and an operator typo in the env value. Rules earned: **(1) When a symptom has a plausible single cause, verify the plausible one is the ONLY one before declaring the diagnosis — check each layer independently rather than stopping at the first sufficient explanation.** The investigation correctly found a missing grant and correctly proved writes had never worked; it did NOT check whether the connection itself was viable, so the fix was right and insufficient. Cheapest general form: for any client→network→auth→permission chain, prove each hop in isolation before attributing a failure to the far end. **(2) An orchestrator brief that supplies a technical premise is supplying an unverified claim, and the agent will build on it faithfully.** This wave's rollback traces directly to a brief that asserted "append-only is by policy; the executor is table owner and bypasses it" — false: the guard was a TRIGGER, and triggers bind every role including the owner (RLS is what owners bypass). The dev implemented exactly what the brief justified. Briefs should cite the mechanism's definition (grep it) rather than characterize it from memory — the Law-2b family applied to the orchestrator's own prose. **(3) The isolation test is the fastest instrument for operator-side failures:** having the operator run the exact credential standalone (psql with the same string put in the config) split "credential wrong" from "config copy wrong" in one command, after two rounds of theorizing. Reach for it the moment a failure could live on either side of a boundary you cannot see into. Scoreboard: 3 root causes, 4 branches, live-verified end to end (first successful UI writes in the project's history), 2 tickets, DSN-classification hardening shipped so this failure class names itself forever after.

- **2026-08-29 · MetaCortex Wave 3 (6 dev slices + 1 content slice + opus gate + 3 micro-rounds + an orchestrator wiring commit; WAI-472/473/474/476/477 safety + WAI-483 stages 2-3 + WAI-487 skill).** Largest wave to date; zero merge conflicts across six branches, seam-pinning validated a third time (two independent producer/consumer pairs, zero drift — including a slug convention one agent GUESSED and the gate proved matched byte-for-byte). Three additions: (1) **Consolidated wiring debt is a first-class GATE OUTPUT, and the orchestrator closes it with a dedicated wiring commit + wiring PINS.** Three of six slices shipped correct, mutation-proven, DORMANT code (zero production constructors — Law 11's build-without-mount at wave scale). The gate's numbered wiring-debt list became micro-round briefs plus one orchestrator-performed commit threading everything through config/main, pinned by AST tests (per the repo's own test_approvals_resume precedent) so deleting either half of any wire reds a named test. Law 11 corollary: when slices are briefed report-wiring-instead-of-editing-config (to keep composition uncontended), the WIRING ROUND is a planned wave phase, not an afterthought — schedule it at dispatch time. (2) **A dev agent's "fix the finding as prescribed" can be wrong by house convention** — the gate flagged HTTP-200-on-failure as a defect; the fix round checked sibling routes first, found 200-always is the documented cross-route convention, kept it, pinned it. Law 9 extends to remedies: reproduce the PRESCRIPTION against house precedent before applying it. (3) Environment facts: the MCP DDL apply path can be classifier-blocked for ALTER-bearing migrations even when a prior CREATE-only one sailed through — author migrations self-verifying (preflight + in-transaction DML drills) and route live applies to the operator psql block by default; and per-slice venvs exhaust the shared /sessions disk in big waves — brief agents to build venvs in /tmp and to delete only REGENERABLE artifacts when clearing space (one agent deleted the orchestrator's baseline venv; harmless only because venvs are cattle). Scoreboard: 9 commits across 7 branches, final integration 4635/6 green + ruff clean (baseline 4415/6), 27 seam probes + 4 wiring pins persisted, gate found 1 P1 (stale pre-merge test) + 4 P2 (three dormancy, one deploy-order FK trap) + 3 P3 (one confirmed-real fail-open sibling in a second subsystem, fixed same wave), 5/5 independent mutation spot-audits red correctly, 2 ticket claims refuted by reproduction and recorded.

- **2026-08-28d · WAI-488 cross-repo UI wave (migration self-performed + 2 sonnet devs + opus gate + 2 micro-rounds; MetaCortex DDL + WaiveBoard panel).** Seam-pinning-at-dispatch (2026-08-28b) validated a second time on a producer/consumer pair split across HTTP: zero drift, merged tsc resolved all 14 pre-merge consumer errors exactly. New mechanics worth keeping: (1) **a mounted repo's `node_modules` is platform-poisoned for the sandbox** — macOS-installed native bindings (rolldown et al.) crash test runners on Linux; the fix is installing the matching `-linux-<arch>-gnu` binding package `--no-save --ignore-scripts` alongside (mac binding untouched, lockfile untouched) — never `npm i`/`npm ci` on the shared tree; check `cpu`/`libc` in the EBADPLATFORM error before picking the package. (2) **A QA "DEFECT:" probe that pins a defect's EXISTENCE goes red the moment the fix round lands** — when a gate records a defect as a failing-shape assertion, the fix round's close-out includes flipping that probe to pin the FIXED behavior (retire-superseded-probes applies to the gate's own artifacts, and the probe file may duplicate its validation tables inline per-test — grep the whole probe file for the stale pattern, not just the shared helper; the inline copy cost a second fix here). (3) A finding's prescribed fix can be WRONG by house convention — the gate flagged 200-on-failure as a defect; the fix round discovered 200-always is the repo's documented cross-route convention and kept it, documented + pinned — Law 9's reproduce-first applies to the REMEDY as well as the finding. Scoreboard: 0017 applied live with role-verified grants before any UI code needed it; 2 dev slices first-gate PASS; merged 297/297 + tsc clean; 4 findings (1 P2 investigated-and-kept, 2 P3 fixed, 1 P3 swept); enforcement follow-up filed (WAI-489); zero prod incidents this wave.

- **2026-08-28c · Wave 2 post-merge PROD INCIDENT (MetaCortex deployment 31fe04ef, boot crash-loop, fixed forward in ~25 min).** The wave 2026-08-28b certified as clean crashed the production boot on deploy: the intake mount eagerly called `resolve_memory_client_factory(config)`, which under prod hybrid env flags + a present `OPENAI_API_KEY` constructs the spend-gated `OpenAIEmbedder` with no killswitch to thread → `MissingKillswitch` inside `create_runtime_app`. Three layers of green missed it — dev's tests, QA's 18 real-composition seam probes, the 4413-green merged suite — because every one of them rode the DEV factory path; the composition code's own comment asserted "never raises" from reasoning about the missing-KEY branch while the present-key branch raised. Added the boot/composition corollary to the never-executed-artifact anti-pattern: **a diff touching app composition gets booted once under a production-shaped config with production-truthy env flags before the merge block ships; every `if is_production:` branch the diff can reach must execute.** Second lesson, Law 1 applied to the orchestrator's own merge notes: "zero runtime behavior change" was an unverified premise stated as fact in an operator-facing message — deploy-impact claims in merge blocks get the same evidence standard as slice reports. Fix pattern for the class: resolve spend-gated dependencies LAZILY at dispatch so boot never depends on stage gating, keep the identical refusal fail-closed at dispatch, and pin both directions (prod-shaped compose succeeds; dispatch still refuses ungated) with a negative control that restores the eager call. Also: when a contract deliberately flips eager→lazy, the probe suite's observe-at-compose wiring is superseded — retire it explicitly (premade-factory injection replaced the observing resolver).

- **2026-08-28b · MetaCortex Factory Team Wave 2 (2 sonnet dev slices in parallel + opus adversarial gate; WAI-485 fetch verb + WAI-483 intake stage — producer and consumer of a NEW contract built simultaneously).** Added the dispatch-side seam-pinning rule to pipeline step 3: the orchestrator pinned `fetch(slug, *, workspace) -> Page` verbatim in both briefs, the consumer tested against a local double, merge order definer-first — QA's seam diff came back with ZERO drift, and the gate's headline probe (consumer through the real `build_factory_plane` against the real `InMemoryMemoryClient.fetch`, the pairing neither dev ran) passed 18/18 including tombstone-refusal and 40KB-uncapped round-trip. Also fixed a lineage-merge defect in THIS file: the wave-close bullet still instructed `git worktree prune` while the ban above forbids it — a merged superset can carry a live self-contradiction; grep the merged text for each new ban's TERM before staging (the 08-28 merge checked law presence, not rule coherence). Environment lesson worth pre-transcribing into every brief on cowork-mounted repos: `git worktree add`/`commit` hit unlink-blocked stale lock files (`index.lock`/`HEAD.lock`/branch-ref `.lock`) that plain `rm` cannot remove — remedy is the `mcp__cowork__allow_cowork_file_delete` tool scoped to the SPECIFIC lock file after confirming no live git process; both dev agents lost a round-trip to this independently. Gate value this run: found the P1 a green suite structurally could not (the stage's one declared pinned skill exists NOWHERE — a declaration naming an external resource is a claim about a population outside the repo; verify the referent exists in the deploy path, Law 12's cousin). Scoreboard: 2 branches (`2942a84`, `56e90ea`) both first-gate PASS (B with defects), merged suite 4395+6 green (baseline 4356+6), merge-order byte-identity proven, 6 delete-the-guard mutations all bit with SHA-verified restores, 18-probe seam suite persisted (`9f6b419` on `qa/wave2-integration`), 1 P1 routed to WAI-487, 4 P3s recorded on-ticket, zero fix rounds needed.

- **2026-08-28 · MetaCortex Factory Team Wave 1 (3 sonnet dev slices + opus adversarial gate + 3 micro-rounds; WAI-480/481/482).** Added the shared-`.git` stash/prune bans + brief-transcription corollary to Multi-Agent Git Mechanics, and performed a lineage merge: the `_inbox` staged copy was a THIRD fork (its Law 11 was the byte-snapshot law; it lacked build-mount Law 11, Law 12, and the 08-24/08-25 entries) — this revision is the verified superset (loaded copy + the fork's byte-snapshot law preserved as **Law 13**; pre-merge fork backed up alongside). Wave lessons beyond the new rules: (1) the opus gate again out-earned its cost — it ran the ONLY live pairing of the migration's schema with the reader's SQL (neither dev did) and found by mutation that the load-bearing `team` filter had zero real coverage under 20 green tests; the fix round's sqlite-backed fake that EXECUTES the production SQL verbatim is a reusable pattern for guarding query semantics without a live DB. (2) An invariant keyed on a literal name ("role_class == 'qa'") is evaded by every synonym; QA caught 'redteam'/'compliance' slipping the fresh-context rule — widen to a named set with one obvious place to land the next member, and check the SAME invariant at every layer that enforces it (the DB-side twin was still literal-'qa'; filed, not fixed, per ownership). (3) Sandbox-created worktrees embed sandbox paths in `.git/worktrees/*/gitdir` — they are session-local; sweep them before session end or the next session inherits unusable registrations. Post-merge incident, same run: **rotating a database password while clients are live trips Supavisor's circuit breaker into a self-sustaining lockout** — the runtime retried with the dead credential every ~6s, and each rejected attempt re-armed the 2-minute block, so it could never clear on its own. Documented fix (supabase.com/docs troubleshooting): STOP the clients first, let the lockout lapse (~3 min of silence), then restart with the new credential. Rule: before any credential rotation, enumerate every live consumer of that credential and stop or update them FIRST; a rotation is a mini-cutover, not a settings change. Scoreboard: 5 commits across 3 branches, merged-tree suite 4344 green (+~50 tests), 12/12 prompt byte-fidelity doc→SQL→live-PG, 1 P2 coverage hole fixed + mutation-proven, 2 follow-up tickets filed (by-slug fetch verb; DB-layer invariant widening), 1 orchestrator briefing error (unbrieifed prune ban) caught by disclosure and repaired with one host command.

- **2026-08-25 · ShipCo Sprint 11 Slice A — TWO incidents, both from shared mutable state with no ownership check.** **(a) Two orchestrator sessions ran the same tree, on the same ticket, simultaneously.** The operator did not launch them deliberately; a self-improving-agent harness spawned a track session alongside the live orchestrator. It was caught only because both happened to add the *same symbol names* and TypeScript threw duplicate-identifier errors — had they edited different regions, one would have silently clobbered the other. The dev agent handled it correctly: probed until the tree went quiet (~7 min), removed its own duplicate net-zero, kept the other session's superset, then built what that session had left undone — which was its gate spec, since that code landed with no tests and no report. **Rule: one orchestrator per tree. Parallelism comes from agents under one orchestrator, which is what the slicing discipline exists to make safe. Two orchestrators share no ownership matrix and cannot negotiate one.** Write a session lockfile at orchestrator start and refuse loudly on a live one. **(b) The same class destroyed skill lineage.** This file forked THREE ways: the staged copy carried an unrelated project's history and had lost Laws 11/7b/Sandboxed-Shell; an archived 'stale-staging' file was actually the *richer* copy, wrongly demoted; and the 08-20 patch adding 7c and Law 12 had propagated to none of the three. Root cause: every session across every project stages to one shared `_inbox/skills/<name>/` path and writes its own lineage over whatever is there. **Rule: the three-way drift check (08-08c) is necessary but insufficient once agents can self-improve their own skills — a self-improving skill needs a lineage marker in the file itself, and the publish gate must refuse a staged copy whose law set is a strict SUBSET of either the registry tip or the loaded copy.**

- **2026-08-24 · Football-analytics research wave (5 Firecrawl slices + synthesis, 4 rounds).** Added Research-Round Discipline. Three distinct child-failure modes surfaced: (1) **research-without-write timeouts** — an agent can complete 20 searches and still never write; the write-early contract only works if the brief makes writing the FIRST action, not a step after research "completes"; (2) **synthesis truncation at max_iterations** — one agent asked to produce 3 deliverables + 2 appends in a single run hit the iteration cap after reading inputs, producing nothing; splitting multi-deliverable work across disjoint leaf agents (glossary | graph+appends) with explicit ~10-action budgets fixed it; (3) **full-input-read stalls** — a glossary agent assigned five whole-file reads never got past them; re-briefed to use targeted content search per term instead of sequential reads. Salvage pattern: timed-out runs leave partial files + scratch + transcripts on disk — continuation briefs should point at transcript logs as primary sources (a no-web salvage round recovered a full note from two dead runs' transcripts). Verify every claimed deliverable against the tree: two of six batches reported success while their files were absent.

- **2026-08-23b · ShipCo Sprint 10 Demo Eve Sweep (3 parallel slices + fix round + fresh QA).** Small-wave confirmation of the freshly staged rules: byte-snapshot probe discipline armed and never needed (read-only mandate executable end to end); a dev agent STOPPED at a foreign test-pin wall exactly per protocol and the operator granted cross-boundary ownership for a three-line atomic fix round. New operational lesson folded in: **the operator's own long-running server on the default port makes plain `smoke:demo` fail at BOOT, and prior waves leave orphaned spawned servers on throwaway ports (found one from Sprint 9's QA on :8795) — sweep orphans first, always pass an explicit `SMOKE_PORT`.** Also: brief baseline numbers refuted by the tree for the second consecutive sprint (864/864/0/0 vs briefed 862+2skips) — Law 1 verification-first is now load-bearing ritual.

- **2026-08-23 · ShipCo Sprint 9 Demo Readiness (5 dev tracks in 2 waves + adversarial QA, shared checkout, no worktrees).** Added **Law 11** and **Detached hermes-agent Fleets**. Headline incidents: (1) a QA mutation probe restored by reconstruction instead of byte-snapshot and died mid-recovery, leaving the tree mutated — recovered from the session DB's persisted `write_file` args (Law 11); (2) four phantom test failures traced to stale `dist/` from concurrent track rebuilds — quiet-tree-only certification; (3) all four wave-1 agents hit the invisible default iteration budget and died without reports — `--max-turns` now explicit at dispatch, resumes preserve context; (4) a launcher pointed two agents at last sprint's same-named briefs — namespace runtime files and assert existence pre-spawn. Scoreboard: 8 tickets implemented + verified (861 tests green from 778 baseline, +83; FR-045 gate 19→20 handlers; demo smoke harness 215 assertions/13 steps), M15/M11 mutations now provably red, 20 honest not-yet-run human SR gates tallied, one should-fix product gap (quote/eSign disjoint snapshot-id sets vs accept route) documented post-demo rather than hot-fixed.

- **2026-08-20 · ShipCo Sprint 8 UI/UX wave (4 Sonnet build + 1 Opus adversarial gate + 3 Sonnet fix rounds; 551→778 tests, 11 blocking defects found against a fully green suite).** Added **Law 7c** and **Law 12**. The gate's headline: 25 primary CTAs rendered completely unstyled — no accent colour anywhere in a product whose entire thesis was mirroring the client's visual system — because one font stack contained a double quote. Twelve green tests asserted that button's colour. Also confirmed: **spending Opus on the gate rather than the build is the highest-leverage model allocation in the run** — four Sonnet agents each self-reported success with live curl evidence and each was locally right; only the adversarial pass with a fresh mandate found the class. And **Law 11's converse bit twice**: an agent correctly stopped at a NO-GO boundary and left its screens unreachable, and a second left a dismissal route unwired — both times the boundary was wrong, not the agent.

- **2026-08-19 · ShipCo agentic-SDLC build (six waves: requirements → knowledge graph → PRD/ARCH → 88-story plan → 5 sprints → web tier; ~20 defects caught by gates that dev-green suites passed).** Added **Law 11** (build-and-mount is one slice), **Law 7b** (verify a guard by removing it), **Law 2b** (prove privilege guarantees under the app's actual role), the catalog-over-error-message rule in pipeline step 3, and the Sandboxed-Shell Environments section. Learnings beyond those: **(a) A production-touching artifact that has never executed is an assumption in costume.** Fifteen migrations were written across five sprints — thorough, commented, trusted, and never once run. Executed against a real PostgreSQL for the first time, three failed: a genuine circular dependency between two files (each needs an object the other creates — no reordering resolves it), an RLS generator failing on four registered-but-column-less tables, and the cascade. Treat "the migrations have never run" as a P1 on sight. **(b)** The roles those migrations created were `NOLOGIN` with no connectable user anywhere, so the acceptance criterion was unreachable by fixing migrations alone — a fix plan must be checked for reachability, not just correctness. **(c) The 08-08c three-way drift rule paid for itself:** the staged copy in `_inbox` was NEWER than the copy loaded into the session (it already carried Law 10 and Research-Fleet Discipline from 08-18). Patching the loaded copy would have created a colliding second Law 10 and silently dropped a prior run's work.

- **2026-08-18 · Boutique PE research fleet (nine research tracks + synthesis + three adversarial re-gates).** Added Law 10 and Research-Fleet Discipline. A schema-green 298-entity graph concealed semantic mirrors, unsupported firm metadata, and a duplicate; successive fresh gates drove the final 322-entity graph to zero structural and semantic blockers. Firecrawl proved endpoint/session-specific rather than simply installed-or-broken: authenticated search/scrape worked while keyless quota and async/parse surfaces varied, so capability probes plus operation-level fallback preserved evidence quality. The first opportunity release described all required fields in prose but omitted many typed relations; a six-category relation matrix caught and repaired that builder-facing incompleteness before acceptance.

- **2026-08-17 · DetailAI WAI-253 wiring pass (3 dev ∥ + QA → FAIL → fix → re-gate → FAIL → fix → 2 closing slices; the first wave to need TWO full adversarial cycles).** Two rules earned. **(a) A wiring pass inherits every guarantee the specialists were proven under — and the gaps BETWEEN their guarantees become the new defect surface.** Every specialist was golden- and mutation-proven in isolation, yet the wired agent could narrate "Saturday 6 AM" while holding Monday 8: money had a grounding guard, time never did, and no isolation-level test could see it because the seam (model narration × scheduler state) only exists after wiring. Rule: when wiring proven components, enumerate the CLASSES of guarantee each component enforces (grounded money, grounded time, grounded facts, consent state…) and require the QA gate to attack every class the composition now spans — "all components proven" is the premise most likely to make a wiring gate lazy. **(b) A guard added in a fix round is the least-tested code in the wave — re-gate the fix, not just the findings.** The invented_time guard that killed P1-1 itself shipped a P1: its extractor blocked the word "sun" in a DETAILING shop's core vocabulary (month-prefix wildcard read "decontamination 2" as December 2), converting correct prose into owner escalations on the highest-value flow. The fix agent tested the guard catches inventions (recall) and never tested that ordinary domain prose survives (precision); the re-gate's fresh-eyes probe found it in minutes. Rule: any new deterministic filter ships with BOTH a recall table and a domain-prose precision control, and fix rounds that ADD guards get a re-gate scoped to the new surface as a matter of procedure (this wave: re-gate found 1 P1 + 3 P2 in fix-round code; the originals were all genuinely dead). Confirmed patterns: measured-then-fixed beats reasoned-then-fixed (the url-check slice measured node's URL normalization per obfuscation form before writing the gate); totality maps beat allowlist tests (ACCEPTANCE_DECISIONS as a tsc-enforced Record makes "a sixth intent fails the build" TRUE, where the sample-string test claiming it was vacuous); pre-gate synthesis in eval harnesses must carry expiry — the honest "DOES NOT PROVE the graph calls X" disclaimers were what let the conversion slice be specified mechanically. Scoreboard: 2 gates, 4×P1 + 6×P2 + ~9×P3 found post-"all-specialists-proven"; all fixed with negative controls; goldens 18→21 scenarios (547 checks) now driving one runAgentTurn per turn with zero synthesis; manifest 99→109 mutants, 0 survived; suite 1141 tests 0 fail.

- **2026-08-13c · DetailAI overnight autonomous run, waves 6–7 (11 dev + 2 QA + 8 fix agents across two full Dev→QA→fix cycles; 11 issues closed unattended, 1 advanced, 2 filed; operator asleep).** Three rules earned. **(a) The cross-slice seam is where overnight waves die — freeze not just contracts but UNIT SEMANTICS.** Wave 6's two headline promises (T-24h reminder, after-hours owner ping) both failed at a seam QA had to discover: the outbox had no send-at/dedupe/void surface and quiet-hours deferred owner alerts. Wave 7 repeated the class one level down: the purge's storage postcondition probed a PATH CONVENTION nothing wrote — a vacuous Law-3 proof on a marketed promise, pinned by a TEST that encoded the wrong convention. Rule: when two slices meet at data (paths, keys, causes, timestamps), the orchestrator freezes the exact FORMAT in both briefs, and the QA gate diffs what one side WRITES against what the other side PROBES — a postcondition that has never seen a real artifact of its counterpart is presumed vacuous. **(b) Meta-quality tooling must be inside a gate before its report is evidence** — the mutation harness and conversation-gate shipped with their own tests in NO runnable gate (scripts/ outside every glob); QA proved a gate-check inversion left the full suite bit-identical green. A tool whose own tests don't run is a claim, not a control. Corollary that paid immediately: the harness's manifest-rot detection fired SAME-SESSION when a fix round moved a guard's text — stale-mutant-as-red converts silent unwatching into a loud diff. **(c) Institutionalized mutation testing found 6 real coverage gaps in ALREADY-QA'D code** (five prior gates, each with ad-hoc mutation rounds, still left bands-cap/max-cents/integer-guard/quiet-hours-boundary/DST-direction unwatched) — ad-hoc rounds sample; a manifest accumulates. Environment notes: raw-NUL-byte-in-source recurred twice more (Write-tool artifact when agents intend \\u0000 — byte-scan every wave; the repo hygiene test now covers whole packages); parallel agents on one shared checkout held (11 dev slices, zero ownership collisions, all mid-flight red states resolved by final merged verify); embedded-postgres lacks pgvector (0012-class migrations verify shimmed locally, fully only on Supabase). Scoreboard: waves 6–7 QA found 3 P1 + 12 P2 + ~20 P3; all P1/P2 fixed in-session with negative controls; migrations 0008–0013 applied live with structural+behavioral proofs; suite grew 734→950 tests (0 fail); goldens 17→18 scenarios; mutation harness 71/71 killed in CI at ~7s.

- **2026-08-13b · DetailAI wave 5 (5 Opus dev ∥ + 1 adversarial QA + 3-agent fix round; 6 issues shipped, 3 stale issues board-truth-closed, 2 filed).** Three rules earned. **(a) A row trigger cannot enforce a cross-row invariant under MVCC** — the capacity "constraint" trigger passed every sequential test and QA killed it by pure reasoning: two concurrent writers each sweep a snapshot blind to the other's uncommitted row and both commit. The guarantee-bearing serialization must live INSIDE the DB layer (advisory lock taken in the trigger, not only in the app), and app↔trigger lock-key parity is itself a seam — drift-guard it by EXTRACTING the key format from both sources and comparing, never by eyeballing. Corollary discovered in the fix round: **userspace embedded-postgres runs in the Cowork sandbox** — "SQL is unexecuted text" is no longer an accepted verification gap for migration logic; the fix agent reproduced the race and proved the fix on live PG 17+18. **(b) Concurrency tests must race across the boundary the lock key partitions.** The fake store locked bucket[0] only and the property suite raced same-slot writers only (same bucket → serialized → structurally blind); the deterministic breach was an ordinary 6pm+7pm Chicago pair straddling UTC midnight. When a lock is keyed on a partition, the stampede matrix's load-bearing case is the one that SPANS partitions. **(c) Keyword interception is contextual, not lexical** — an unconditional START/'yes' intercept ate the single most common booking confirmation on the crown-jewel flow; opt-in keywords only mean opt-in when the number is actually suppressed (Twilio Advanced Opt-In semantics). Same class in the probe layer: a cross-insert probe counting ANY throw as "RLS rejected" was satisfied by its own seed's unique-violation — rejection evidence must be SQLSTATE-specific (42501), all other errors are ambiguous and fail the probe. Environment note: 5 parallel devs in one shared checkout produced transient repo-wide red (tsc/tests) as slices landed mid-flight — expected noise; the post-wave merged-tree verify is the only arbiter, and agents' own full-suite runs get filtered to owned paths. Scoreboard: QA found 3 P1 (MVCC race, bucket[0] lock, "Yes" swallowed) + 5 P2 (NUL-byte binary source, threadId∦unique-key, missing send-time recheck, probe vacuity, normalization gaps) + 12 P3; 13-mutation vacuity round found exactly 1 vacuous guard (constant-time compare) — now source-guarded; all P1/P2 fixed in-wave with negative controls; migrations 0005/0006/0007 applied live with HB001/HB002/HB003 + grant revokes proven behaviorally; 455 tests 0 fail.

- **2026-08-13 · DetailAI S1 waves 2–4 (consolidated retro; 2–3 Opus dev + 1 Opus adversarial QA per wave, frozen-seam parallelism, in-wave fix micro-rounds).** Three rules earned. **(a) The lockfile is part of the workspace seam** — a wave that ADDS a monorepo workspace must regenerate `package-lock.json` and prove it with a clean-room `npm ci` in an empty dir before handoff; local verify green is structurally blind to lock drift because node_modules already exists (second instance of verified-locally ≠ verified-in-CI; this one cost a red main). **(b) A new fail-closed guard must be scoped to its input domain or it re-routes the error taxonomy** — an internal-whitespace rejection meant for digit-like strings ("1450 50", the 100x trap) initially swallowed prose ("call for pricing"), changing its code from `not_a_number` to `ambiguous_decimal`; the pre-existing suite caught it, which is exactly why the FULL suite runs after every micro-fix, not just the new regression file. Corollary: when a defect class dies, its neighbors' error codes are part of the postcondition. **(c) Source-guard tests must match syntax, not prose** — a no-HTML-sink guard tripped on a comment reading "never innerHTML"; pin property-ACCESS patterns (`[.[]\s*['"]?innerHTML`), and treat the false positive as the guard proving it fires (then verify against live code, Law 7). Domain notes banked: Postgres column-list REVOKE cannot narrow a table-level GRANT (revoke-table-then-grant-columns is the only sound shape — proven live with `has_column_privilege`); partial-import unit claims poison later hand-typed values — pin units PER CELL, claim globally only on complete coverage; DDL applied live gets a synthetic end-to-end probe (3 signup paths + cascade semantics + grant probes, then cleanup) before the issue closes. Scoreboard across the three waves: 2 P1 (spelled-price bypass; 100x money grouping), 6 P2 (regex overmatch, takeover fail-open, stale-seam test fallout, durationUnit poisoning, widget open() double-session race, role-escalation UPDATE path) — all caught by adversarial QA gates that dev-green tests missed, all fixed and negative-controlled in-wave; 10 P3 batched to backlog; 214 tests, 0 fail; RLS + grants proven empirically on the live project.

- **2026-08-12 · DetailAI S1 wave-1 (3 Opus dev + 1 Opus adversarial QA; frozen-seam parallelism).** One rule earned, folded into Law 5 / the Dev→QA pipeline: **property tests over a natural domain do not cover the adversarial domain — the QA gate must probe non-finite and off-type inputs explicitly.** A dev's green property suite sampled severity ∈ [0,1) and pronounced "the model can never invent a price"; the adversarial gate passed `NaN` and found the guard `sev<0||sev>1` lets it through (both comparisons false) → a corrupt/NaN price marked `applied`, or a downstream schema crash, instead of the promised escalation. The one-line fix (`typeof!=='number'||!Number.isFinite`) closed a second finding (string-coercion) for free; regression was negative-controlled against the QA-documented pre-fix behavior. Corollary reaffirming Law 4: a guarantee's *test domain is part of the guarantee* — "no invented cents" is only as true as the input range the property sampled; state the uncovered domain rather than reading green as total. Also confirmed the **frozen-seam** habit (orchestrator writes the shared contract before dispatch; dev slices conform, QA diffs emit-vs-read field by field) prevents seam races cleanly across 3 path-disjoint slices. Environment note (new this run): **no SendMessage in the Cowork harness** — sub-agents can't be resumed for fix rounds, so small post-gate P2s become orchestrator micro-rounds with orchestrator self-verification (proportionate, per the existing micro-round rule); and the orchestrator ate its own dogfood twice (a JSDoc `any` and an `unknown`-arg cast) — orchestrator-made test edits are held to the same tsc gate as agent work. Scoreboard: 1 P2 caught by QA that dev's green tests structurally could not see, 1 P2 (untyped-category CHECK) caught, 1 P3 dissolved by the P2 fix, 3 P3 + 1 security-note routed to backlog, 0 seam breaks, 42 tests green.

- **2026-08-12 · WAI-48 approval queue (2 repos, 4 QA campaigns, live drill, 1 hotfix).** Three learnings. (1) *Law 4's deepest form:* the live drill caught `PgOpsClient.notify` missing — the fake implemented it, six call sites called it bare, two hid it behind `hasattr`, and crucially the PROTOCOL never declared it, so nothing structural could notice. Rule: for every protocol/fake/real triple, ship a conformance test in all four directions (protocol→real, fake→real, fake→protocol, protocol→fake) with signature comparison — fake→real is the load-bearing direction because it catches methods the protocol forgot. Treat any `hasattr(client, method)` guard on a seam as a finding in itself: it is how this class hides. (2) *Law 6 extends to BUILD backends:* an unpinned hatchling (installed fresh inside the Docker image build) shipped a stricter release and broke the production build with ZERO repo change (rejected `readme = "../README.md"`). Redeploys mask this for weeks — only real rebuilds exercise the floating dependency. Pin build-system requires, and treat "the build worked last week" as evidence about last week's toolchain, not this week's. (3) *Orchestrators eat their own trap food:* the data-modifying-CTE snapshot rule (already in this skill's history) bit the ORCHESTRATOR live — INSERT in a CTE + UPDATE of that row in one statement silently no-ops the UPDATE, and a 2s worker poll claimed the half-configured row before the repair round-trip landed. Drill-recipe rule: when arming a row a fast consumer can claim, insert it UNCLAIMABLE (a park status), configure it fully, then arm it with a separate plain UPDATE. Also reconfirmed ×3 in one evening: operator env-var paste traps (stale password; psql command tail glued into a DSN) — instructions must show the value with an explicit end marker ("ends at /postgres, nothing after"). Scoreboard: gate 1 caught 2 P1s (an unapproved gate could execute), the re-gate caught 2 P2s introduced BY the fix, the third gate went GO, and the live drill still found one more defect no gate could reach — every layer of the pipeline earned its keep on a single ticket. Suite 1443 → 2038; both drill variants $0.00.

- **2026-08-08c · Lineage merge.** The skill had forked three ways: the account/loaded copy carried Laws 7–9 (compressed), Operator-Runbook Execution, and Eval-Wave Rules; the registry carried the 07-28 BT Journal entry, the WaiveBoard deploy entry, and updated slice-templates; a stale staged dir (from 08-04) was then published over the newer registry tip, regressing it. That revision was the verified superset of all three. New rule: **a staged skill dir is itself a premise — before publishing, diff staging against the registry TIP and the loaded/account copy; check-drift is three-way, not two-way.**

- **2026-08-08b · P1 vault remediation wave (4 parallel dev + QA; all slices first-gate PASS).** Learnings: (1) **Content-vault waves parallelize on path ownership alone** — no worktrees needed when slices are disjoint by directory and nobody commits; the QA gate's ownership-matrix check (every modified path maps to exactly one slice) is the load-bearing control. (2) **Read-path indexes double as drift detectors** — enumerating a folder against its hub exposed a same-morning exporter/hub divergence no one was looking for. (3) **Name cross-deliverable interactions at dispatch:** a staleness sweep that preserves `updated:` and a sync script that rewrites `updated:` from mtime are mutually clobbering in the same wave; check each slice's postconditions against every OTHER slice's planned side effects before running batch normalizers.

- **2026-08-08 · P0 vault remediation wave (2 dev + 1 QA + orchestrator runbook).** Learnings: (1) **Law 7 mechanism catalogued: a bash heredoc clobbers piped stdin** — the orchestrator's own pre-commit YAML guard read `''` from stdin and passed everything while its sibling guards worked; per-guard poison probes caught what a combined probe would have missed. Guards written and tested by the same party stay vacuous. (2) **Cowork sandbox silently blocks `unlink`** until delete permission is granted — git init half-fails with stale index.lock; the clean recovery is delete-permission → `rm -rf .git` → re-init, not lock surgery. (3) "Recoverable via baseline commit" is a *claim requiring the file be tracked* — verify with `git ls-tree` before promising recoverability of a planned deletion (Law 1 corollary).

- **2026-08-07 · Obsidian multi-agent vault research wave (4 research agents + 1 auditor, fully parallel).** Learnings: (1) **Law 1 applies to audits, not just destructive acts** — the auditor's first pass computed a 5× wrong broken-link rate by trusting the vault's own SCHEMA.md for the root path; it self-corrected only by re-deriving the premise from live config. An audit's frame-of-reference must be discovered from live state, never from the subject's documentation. (2) **Research slices partition cleanly by question-domain with explicit NO-GO topics.** (3) Load-bearing external claims that post-date orchestrator knowledge get one primary-source re-verification before entering the deliverable.

- **2026-08-06 · Blind identification wave (4 identifiers + QA).** Added Eval-Wave Rules. Spec-authoring defects dominated agent error 9-to-2 in adjudication; agent-recorded suppressed candidates recovered 3 matches; a phantom row proved no-trade labels are absence-of-log artifacts (labels need positive confirmation); data-first extraction outperformed vision.

- **2026-08-04 · WAI-157 universal killswitch (2 repos, 2 dev slices, QA gate → composition FAIL → fix round → re-gate GO → guard round → live drill).** Three learnings. (1) *Law 7 sharpened:* assertions that grep a RAW file or `inspect.getsource()` are satisfiable by comments and string literals — QA proved a SUPERUSER mutation passing because prose named the attributes, and `autocommit=True, # autocommit=False` passing a source-grep while the timeout failed open. Assert against the comment-stripped executable text or the CAPTURED call kwargs, never source text; and the comment-stripper itself must be quote/dollar-quote-aware or a `-- ` inside a string literal cloaks any statement. (2) *Law 10 instance, cross-REPO:* two repos shipped contradictory GRANT instructions for one credential — whichever ran last would win and neither review would see the union. Rule: one artifact is the single source of truth for a credential's entire privilege set; the other repo ships zero grant statements and says so. (3) *Law 2 instance:* Vercel serverless pools × Supavisor session mode = every lambda instance camps on session slots until reaped (all 15 held idle by one role; the new credential couldn't connect at all). Web-tier pools go through transaction mode (:6543); unjam live with pg_terminate_backend on the idle set. Also reconfirmed twice: operator placeholder traps (PASTE_PW pasted literally; a whole psql command pasted into an env var) — the loud-failure design caught both, which is the point. Live-drill scoreboard: migration + role + RLS policy pair applied and forge-proofed live (5f), full KILL→RESTORE cycle audited, 1443 tests, ~60 checker mutations to zero survivors.

- **2026-07-30 · Operator-gated cutover, orchestrator-executed.** Added the **Operator-Runbook Execution** section and **Law 3d**. Discover live state before following the plan — the runbook's dominant risk had expired, making the window ideal rather than risky; a fail-closed build deployed ahead of its data migration refused readiness for 28h *harming nothing*. 268/318/863 rows copied with all gates green, probe run succeeded with source frozen.

- **2026-07-29d · Dual-repo deploy + QA re-gate.** Extended **Law 1** with the handoff-summary-is-an-unverified-premise corollary — a resumed summary claimed "merged to main" while the checkout sat on a stale `main` with the target file absent. Added the device-bridge/FUSE commit subsection. Verified two new deps were *declared in the manifest*, not merely imported. Scoreboard: 4 security holes closed with negative-controlled tests + 0 mutation survivors.

- **2026-07-29c · Feature wave.** Law 9 (reproduce QA findings — measurement method is part of the claim); Law 3 staleness, Law 4 value-laundering, Law 7 poison-positioning, Law 8 over-harvest controls; brief properties not prescriptions; micro-rounds. Stale-venv trap caught the orchestrator. Scoreboard: 1 unauthenticated-wedge caught pre-merge, 1 QA finding refuted, 11 evasion spellings closed, 3 vacuous guards replaced.

- **2026-07-29b · Migration + guard wave.** Laws 7–8 (whole-artifact greps pin nothing; 22 mutations → 7 survivors → 10 more, incl. a tautology-rewritable verification gate); Orchestrator-Performed Edits (a self-made rename left seven stale references QA caught); pipeline hardening. Scoreboard: 1 blocking defect caught by QA, 1 more by the fix round, 17 guard gaps closed by mutation, 3 orchestrator briefing errors corrected by dev agents.

- **2026-07-29a · Build cycle (6 orchestrations).** Laws 1–6; Dev→QA pipeline; git mechanics; holdout hygiene; board discipline; this loop. Scoreboard: ~10 would-be production defects caught pre-merge, 2 post-deploy by live evidence, 1 orchestrator error caught by QA.

- **2026-07-29 · skill-publish gate adopted.** Registry writes flow through the staged publish process, converging the disk registry, account store, and staging copy that had drifted three ways.

- **2026-07-28 · Agent-registry wave (2 dev + 1 QA-concurrent).** **(a) Run QA CONCURRENTLY with dev when its mandate is read-only** — the audit found a P0 (wildcard CORS + zero auth → proven read + key-exfiltration chain) while feature slices were still being written. Enforce with a written "you may not modify a single line of code" clause. **(b) When two slices build against one contract in parallel, gate the SEAM explicitly.** **Environment:** on a FUSE-mounted repo, *any* index-touching git command can leave an undeletable `.git/index.lock`.

- **2026-07-28 · BT Journal agent-registry wave (3 agents parallel: 2 dev + 1 QA-concurrent).** Two additions earned. **(a) Run QA CONCURRENTLY with dev when its mandate is read-only and its scope is the untouched remainder of the repo** — the audit found a P0 (wildcard CORS + zero auth → proven journal read + API-key exfiltration chain, reproduced on the operator's exact pinned FastAPI) while feature slices were still being written, so the fix shipped inside the same wave. Enforce with a written "you may not modify a single line of code" clause. **(b) When two slices build against one contract in parallel, gate the SEAM explicitly**: diff the keys the backend actually emits against the fields the frontend actually reads, and hand-check any field one side derives rather than receives. **Environment:** on a FUSE-mounted repo, *any* index-touching git command — including `git status` — can leave an undeletable `.git/index.lock`; use `find`/`ls`/`diff` for state and let the operator own git invocations there.

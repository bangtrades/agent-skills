# Changelog entry — 2026-09-05b software-excellence-kg

Appended to SKILL.md (staged copy, verified superset of the loaded copy: 12 laws, 35 changelog entries). Source run: cortana-vault/research/software-excellence-graph/ACCEPTANCE-REPORT.md.

- **2026-09-05b · Software Excellence Knowledge Graph (9 parallel Opus Firecrawl research tracks + 1 opportunity track + 2 Opus synthesis writers + 1 Opus read-only QA gate + lead fix round; 249 entities / 1,148 edges strict PASS; gate: PASS with defects — 7 blocking, 15 should-fix, 2 partly refuted).** Three additions. **(a) A minimum-degree floor plus a type-constrained predicate with a thin target population manufactures a SINK.** `mitigates → risk` had ~10 valid targets and every track needed ≥3 relations, so 33 of 78 `mitigates` edges landed on `technical-debt` — 17 of them effectiveness claims no entity supports (OKRs, Shape Up, Tailwind, htmx "mitigate technical debt"). The compiler saw a healthy graph. Rules: at bootstrap, count valid targets for every type-constrained predicate and either seed the target population in the anchor wave (risk and metric entities are cheap and load-bearing) or tell agents the floor may be met with `references`; add a per-predicate in-degree concentration check to the Law 10 gate (any single target taking >25% of a predicate's edges is a sink until read). **(b) Polarity predicates cannot be type-enforced — enumerate their targets.** `solved → risk|concept` let nine events "solve" `product-market-fit` and `aha-moment` (goal states, not pain points) and let a retraction "solve" technical debt. Where a predicate encodes polarity (solved, mitigates, triggers, supersedes), the SCHEMA restricts the target to `risk` or to a named id list; `concept` is never a valid target for a polarity predicate. **(c) Deterministic mirror resolution is unsafe for causal pairs.** My drop-the-weaker-predicate rule scored `mitigates` above `constrains` and so kept the REVERSED edge (`multi-tenant-saas-architecture mitigates data-breach`) while deleting the correct one (`data-breach constrains multi-tenant-saas-architecture`); the QA gate found the survivor from the entity's own body text. Specificity ranking is a proxy for truth only on non-causal pairs; any pair containing mitigates/triggers/solved/produces gets its direction read from the body before an edge is dropped, and the dropped list is itself a QA input (which is how this one was caught). Reconfirmed: the QA gate spent well — 12 of 13 load-bearing dated claims verified live and one refuted (`measured_by` bound an RCT's task-completion time to DORA lead time); Law 9 reproduce-first partly refuted two findings (7 of 16 `solved` edges were legitimate; the Vogels byline was correct per page metadata) and saved two wrong edits; the write-early contract again produced 9/9 complete tracks with 0 dead runs; agents correctly refused Firecrawl JSON-mode dates fabricated on pages with no visible date (Playwright, Rails) — JSON extraction of a date that is not on the page is a hallucination surface, cite page metadata (`article:published_time`) instead. Mechanics: a generator emitting `\|` for wikilink aliases must know whether the line is a table row (escape) or prose (do not) — 399 prose links carried a backslash into the target (Law 7c: check the emitted markup in the context its consumer parses); on this FUSE mount `.git/index.lock` could not be unlinked and the pre-commit's pinned Python 3.14 runtime is absent, so the operator owns the commit — the vault contract validator can still be imported and run per-file with a negative control to prove it bites.

# WaiveBoard S01 — September 7, 2026

Run: wb-s01-20260907-01. GPT-5.6 Sol implementation with separate Sol core and invitation/browser QA; root verification. Four planned tickets plus a narrow separately deployed Summer Fridays caller repair. Draft PR: https://github.com/waivelabs/waiveboard/pull/4. Candidate at retro: f7cd88c0ffee47aaf754c34ffb56ac2ab51f172c. Sprint acceptance remains unapproved because a required constructed portal-action replay was stopped by the agent service safety filter; no workaround or coverage substitution was accepted. Ordinary browser checks continue separately and their completion cannot certify the rejected probe.

No new law: the existing laws covered the failures. Three concrete reconfirmations:

1. Test the served artifact and the intended failure checkpoint. Next's generated type validator could mask an async-prop mismatch through `any`; populated npm-start routes supplied the real proof. One process-cleanup probe initially failed before starting its second server, so it did not prove the claimed cleanup. A later test cleanup recursively removed a tracked declaration; the root caught the destructive assumption and required per-file byte snapshots/restoration. A deleted tracked input is not proof of a clean-checkout compiler defect.
2. Pin the actual predecessor and distinguish environment fidelity. Current database state differed from migration history; sanitized predecessor plus explicit drift and extension setup were required. Supabase Auth/PostgREST proof and exact PostgreSQL/vector SQL-version rehearsal establish different claims. Catalog comparisons need stable object identity rather than locale-sensitive ordering. An independently deployed legacy caller required its own verified compatibility patch and release order before shared RPC changes.
3. Treat reviewers as fallible and preserve artifact versions. Root/QA alleged a separate request-form bypass; the predecessor already mounted RequestsPanel and disproved it. QA's overbroad disabled-internal-admin and accepted-history expectations were corrected against approved scope. A real relink defect survived green CI: replacing Auth identity A with B retained A's membership until a same-author atomic repair. Independent post-revoke failure injection then proved rollback. A later invitation denial produced an unhandled 500 with zero privileged effects; its narrow typed-error repair passed focused tests and root build/typecheck/781 tests. Sealed reports remain immutable; a report edited in place after handoff was acknowledged, not reconstructed from hashes.

Root's normal browser smoke verified member request submission, client-admin task/deliverable acceptance, correct database actors and certificate creation. A duplicated task card was filed separately as WAI-726. Existing release gates, production qualification and full client acceptance remain distinct. The run report/evidence folder is /Users/nolan/Projects/agency/WaiveLabs/waiveboard-sprints/wb-s01-20260907-01.

Three-way lineage review found registry/account byte-identical and staging richer; all existing stage additions were preserved. Registry and account symlinks were not written. Operator publication: ~/Cortana/cortana-skill-registry/bin/publish-skill.sh orchestration

---
name: orchestration
description: "Coordinate complex software work across multiple agents — plan disjoint slices, dispatch dev agents, run adversarial QA gates, verify all claims against evidence, and manage tracker/board state. Trigger on \"orchestrate agents\", \"dispatch agents\", \"run the sprint\", \"coordinate a build\", \"agent slices\", multi-agent dev work, or any request to plan/execute/verify parallel engineering work. Review this skill when a run produces an evidenced reusable lesson."
---

# Orchestration

## Cortana SDLC and delivery playbooks

For Cortana/WaiveLabs software work, read the **work breakdown and Linear** route in `cortana-vault/projects/claude-skills/claude-skills--development-sdlc.md` from the accessible Cortana root (local default `~/Cortana`) or approved project export before making the corresponding decisions. It links the maintained SDLC starter and MetaCortex playbooks. If unavailable, name the missing context and proceed from supplied evidence only where the next decision does not depend on it.

Use the current project plan and MetaCortex execution context when applicable. Bind PRD/architecture versions, task and verification DAGs, real integration seams, exact release/recovery and the usable-MVP feedback loop. Preserve existing authority and independent verification capacity.


Coordinate complex software work across multiple agents, contributors, or implementation slices. Convert messy project state into clear parallel work, truth-based reporting, and a release path that never confuses activity with progress.

Project-agnostic: discover the repository, sprint plan, local instructions, test gates, tracker conventions, and report formats from the active working context.

## Core Rule

**Orchestrate from evidence, not optimism — and that includes your own claims.** A slice is complete when code, tests, gates, logs, tracker state, or an explicit operator result prove it against acceptance criteria. An orchestrator's diagnosis is a *claim* like any other: before any destructive or irreversible action, the premise itself must be independently verified (see Law 1).

## The Laws (earned in production — each one cost something)

**Law 1 — The orchestrator is fallible; verify premises before destructive acts.**
Incident: an orchestrator concluded live checkpoint tables were "inert" from `max(thread_id)` over a *text* UUID column — a lexicographic max that says nothing about recency — and queued an archive that would have destroyed running production state. An independent QA agent overturned the premise minutes before execution. Rule: any archive/drop/migration/teardown gets its *premise* re-derived by an agent who didn't form it, from raw evidence. Prefer reversible forms (rename/archive-schema over drop; disable over delete) even after verification. Corollary: a compaction/handoff summary is itself an unverified premise — re-derive branch/merge state from live git before committing on a resumed session's word.

**Law 2 — The environment silently discards configuration; verify guarantees empirically, never structurally.**
Incidents: a connection pooler (Supavisor) silently dropped `options=-csearch_path=...` startup parameters, rendering a schema-isolation control inert while every health check stayed green; `uv pip compile` preserves existing pins when the output file exists and fresh-resolves when it doesn't; GitHub's `required_approving_review_count: 0` silently disables the review machinery a rule appears to enable. Rule: any guarantee mediated by infrastructure must be proven by observing the *live effect*, not by reading the config that requests it. Corollary: the fix must sit at a layer the environment cannot drop — an in-session statement beats a startup option. **Corollary 2b — prove the guarantee under the role the application actually uses.** `REVOKE ... FROM PUBLIC` does not bind the table owner and PostgreSQL never permission-checks a superuser, so seven tables' append-only guarantees were inert while the app connected as owner; a `DELETE` succeeded against a table whose privileges forbade it. The one control that held was a trigger, because a trigger binds regardless of role. A privilege-based guarantee tested as superuser proves nothing.

**Role-scope corollary (S01 continuation, 2026-09-06): a signed transaction context proves freshness, not the caller’s authority to choose its workspace.** Test that the binding function authorizes the actual connecting login before minting the context, and that the production connection uses that login. Rehearse migrations with the real predecessor security-definer bodies; dummy permissive bodies can hide role and schema failures. If the original baseline is missing, record the reconstructed portion and keep full-chain qualification open.

**Law 3 — Success output must assert the postcondition, not completion.**
Incident: a deploy-prep script printed two success lines while its `setup()` no-op'd against the wrong schema. Rule: every script/gate/probe that reports success must verify the *resolved state* and fail closed otherwise. Extensions: a readiness value computed once at boot is a snapshot, not a check (3b); a verdict past its staleness bound reads un-ready (3c); and for data migrations the postcondition ladder is *structural parity → copy-count identity against recorded preflight numbers → a live-run probe with the source proven frozen* — the last rung (source count unchanged while the new path grows under real traffic) is the only one that proves the cutover rather than the copy (3d).

**Law 4 — Fake-fidelity: every external-SDK seam needs one test against the real client.**
Incident: an agent added a `usage` kwarg asserted by 19 tests — all faking the client one level below the seam; the real SDK's method had no such parameter and every live model call crashed. Rule: one test per seam constructs the REAL pinned client through the production code path. Watch shape (`ChatResult` vs `LLMResult`) and watch *library value-laundering*: a pinned client that back-fills absent fields with zeros can convert "no evidence" into "affirmative zero" one level below your check. **Corollary 4b — installed-version check (2026-09-07):** before accepting a test report, compare the executing environment's installed distribution versions with the active lock and record the imported source path — a correct lock file does not update an old virtual environment. Reproduce SDK-dependent controls with the actual locked client before assigning a release verdict.

**Law 5 — Deterministic gates before subjective judgment.**
Hard-loss pre-checks run before the LLM reviewer; no model opinion overrides them. In orchestration: file lists, pairwise overlap, parent SHAs, byte-identical merge order, protected paths, lock freshness — before any subjective review. Watch for judge anchoring (a reviewer returning the same score every time is not discriminating).

**Law 6 — Pin the toolchain; CI installs what production installs.**
Floating dev tools broke CI on upstream releases while production stayed green on the hash-pinned lock. Exact-pin dev tools, semver-guard runtime deps, CI consumes the production lock artifact.

**Law 7 — A test that greps the whole artifact pins nothing. Scope assertions to the block that must carry the property.**
Incident: a migration's re-key assertion was satisfied by sibling sections even when the copy didn't re-key; fixed in §3, immediately recurred in §4. Extension: in layered defenses, *position the poison where only the guard under test can stop it* — a charset assertion whose control characters sit past the length clamp is satisfied by the length clamp alone. When a dev self-reports replacing a vacuous test, verify the replacement bites. **Extension 7b — verify a guard by REMOVING it, not only by poisoning past it.** The orchestrator mounted a control, wrote a test proving refusal, watched it pass, then unmounted the control and watched all tests pass anyway: a pre-existing weaker guard refused first, so the stronger one could have been deleted entirely with a green suite. **Two controls in series means the later one is untested until the earlier one is removed.** Delete-the-guard is the cheap negative control and it catches what poison-positioning misses. **Extension 7c — assert against the PARSED artifact, not its source text.** Twelve unit tests asserted a button's `background`, `color` and `font-size` via `assert.match()` on raw HTML and all twelve passed while every one of those declarations was being *discarded by the browser*: the component interpolated a font stack containing `"Segoe UI"` into a double-quoted `style` attribute, and the embedded quote terminated the attribute early. The bytes the test grepped for were present; they had simply stopped being part of any attribute. **A string-level assertion cannot see a syntax error that changes meaning** — parse the artifact the way its consumer will (HTML tokenizer, JSON parser, YAML loader) and assert against the parsed value.

**Law 8 — Mutation-test the guards on anything that will touch production data.**
38 mutations on one migration → 17 initial survivors including a verification gate rewritable into a tautology. For governance *scans*, also mutation-test the loosening direction (over-harvest controls): pin the false-positive boundary too.

**Law 9 — QA findings are claims; the fix round's first act is to reproduce them.**
Incident: a re-gate reported a P2 fd leak with a measured repro. The fix-round agent reproduced it first and found the measurement was an artifact — the blackhole listener lived *in-process*, so every accepted connection added a server-side fd to the same process's count; a true out-of-process blackhole leaked nothing. The prescribed restructure was built, measured, and shown to move nothing. Rule: a QA repro's *measurement method* is part of the claim. Ship the stronger regression test anyway when the class of gap is real; record the refutation so the finding doesn't resurface.

**Law 10 — A structurally green knowledge graph is not semantically green.**
Incident: a 298-entity research graph passed schema, source, and connectivity gates while still carrying cross-predicate mirror edges, reversed risk/control semantics, unsupported firm-observation metadata, and one true duplicate. Rule: before synthesis, run a fresh semantic gate that attacks one-fact/two-direction pairs, predicate domain/range, source-to-metadata traceability, and conceptual duplicates. Minimum degree counts only distinct, live, non-self relations; removing unsupported edges must trigger re-validation rather than relation padding.

**Law 11 — The slice that BUILDS a control must also MOUNT it. Splitting build from wiring produces finished, tested, unreachable code.**
Incidents (four in one project, each caught by a different gate): a routes module left as a six-line stub, so activation and deactivation had passing domain tests and no route; three platform controls — a disclosure gate, a config-driven branch, a notification recorder — shipped green with **zero callers**, so a direct API call skipped the gate entirely; a route that reached a stub returning a hardcoded refund while the real service, twenty passing tests deep, had no production caller *and* wrote to a different store than the command that fed it; five UI screens each defaulting to their own empty service, so the one that could never find a policy returned "not found" forever. Every instance shared one cause: **the mounting service was NO-GO for the slice that built the thing being mounted**, and every agent obeyed its boundary correctly. The boundary was wrong. Rules: (a) ownership boundaries must not separate a control from its call site — if they must, the wiring is its own slice dispatched in the same wave, never "someone else will"; (b) a slice's definition of done is **a request that reaches its code through the real seam**, not a passing unit test; (c) the orchestrator's smoke boots the artifact the way the operator will (`npm start`, not a hand-constructed harness) — constructing the server yourself with different options is how you prove a screen works that is dead in production.

**Law 12 — A gate that grades a registry passes; only a gate that grades the artifact bites.**
Incident: in one build every gate keyed to a declared list reported green while the rendered product was broken. The contrast gate graded a token table, not the emitted CSS, so cyan shipped as body text at 3.32:1. The conformance gate hand-rolled the flow list two lines after a comment forbidding exactly that, so no new screen was ever covered. The hex-literal gate cited "no hex in a **screen** file" and scanned only the components directory. Nothing asserted that a served page contained the script whose hash the CSP pinned. Rule: for every gate, ask *what would have to change for this to go red* — if the answer is "an entry in a list I also control," it is grading the declaration, not the thing. Point at least one assertion at the rendered/served/compiled output. Corollary: **a fixture smaller than production's smallest real case proves nothing about production** — two tests pinned a last-vehicle rule with one seeded vehicle where the demo seeds two, and the rule was wrong for every sequential removal. **Extension 12b — reviewer-side (2026-09-18): a score that rises when the reviewer looks at less is grading the review, not the artifact.** Any scoring instrument a wave builds gets an adversarial case where the reviewer withholds effort; "not verified" scores 0 and keeps its full weight with coverage reported separately, and applicability is a property of the artifact class, never of the review. **Extension 12c — rendered media and prose (2026-09-15/16/19):** assert the viewer-facing property — rendered box ratio via `getBoundingClientRect`, fraction of the picture shown, the content not its container (a homography that maps a bounding box, `object-fit: cover`, or a presentational `height=""` all hide a wrong box as a plausible crop); run the adversarial gate only after real media has landed (a green gate on placeholders is a claim about placeholders); include a keyboard walk (Tab through every focusable, then compare geometry to a clean load — script-positioned viewports must be `overflow: clip`); report a Lighthouse score with its invocation and throttling config; and in content-vault waves grep every asserted timestamp back to a machine-readable stamp inside a cited document — a time that exists only in `stat` output or a heading is not a claim any source makes.

**Law 13 — A mutation probe snapshots BYTES before it mutates; a hash alone cannot restore.**
Incident: an adversarial QA agent popped a record entry to prove its gate red, recorded only the pre-probe SHA-256, then reconstructed the deleted object from memory — restore failed byte-identity and the session died mid-recovery, leaving the tree mutated. Recovery succeeded only because the session DB persists agents' literal `write_file` arguments. Rule: before any transient probe, `cp` the target aside; restore is *copy back*, verified by SHA against the snapshot — never by reconstruction. Corollary: the session DB (`messages.tool_calls`) is the authoritative recovery source for every file an agent wrote, including contents orchestrators never saw. *(Lineage note: this law existed as "Law 11" in a forked staging copy; renumbered 13 in the 2026-08-28 lineage merge so the build-and-mount Law 11 and registry-gate Law 12 keep their numbers.)*

## Token-efficient execution

These rules bound orchestration overhead; they never relax user instructions, acceptance criteria, required tests, independent QA, or safety and authorization boundaries.

1. **Budget the wave and observe consumption.** Before dispatch, record the intended outcome, model/effort, maximum concurrent agents, repair allowance, and next time checkpoint in the existing run receipt. Honor user-set time/token/usage limits across the root and all workers; reserve room for QA and closeout. When no limits were supplied, use a 30-minute progress checkpoint and at most two unsuccessful repairs of the same invariant before reassessing the affected slice. These are reassessment triggers, not permission to abandon authorized work or skip gates. At a trigger, report evidence gained, remaining uncertainty, and the smallest bounded next step; stop redispatching the unchanged approach. Continue independent authorized work. Use native hard limits when available; otherwise label limits advisory, inspect usage at dispatch/return/checkpoints, and mark unavailable measurements unknown. Record input/output/cached/reasoning fields in the provider's native schema without double-counting. Account-wide quota deltas are coarse signals, not per-run attribution. Do not silently expand an explicit operator budget or build a new telemetry system merely to measure this run.

2. **Send the minimum sufficient context and use the smallest adequate team.** Give each worker a task-specific brief: outcome, exact acceptance criteria, owned paths, relevant source identities, known failure, required checks, authority/NO-GO rules, and the next decision. Link evidence instead of copying transcripts, full logs, incident histories or unrelated skills. Prefer fresh task briefs over full-history forks; reuse the same producer/reviewer for bounded fixes when their context remains relevant, preserving reviewer independence and private-case isolation. Aim for briefs under 800 words and returns under 250 words plus artifact links; exceed these soft targets for necessary contracts or evidence, never omit constraints to meet them. Summarize tool results with counts, failures and pointers; retain raw output outside prompts. Add an agent only for independent work that shortens the critical path. For a single dependent slice, start with one producer and one independent reviewer. Follow the user's model policy; escalate for demonstrated reasoning complexity, not routine file copying, hashing, board updates or environment waits. Batch mechanical work with scripts. Summaries are navigation aids, not proof of source state.

3. **Make each review or rerun answer a new question.** Before expensive model evaluation, run a small end-to-end preflight through the real producer/consumer seam and check prompt-to-rubric consistency with synthetic positive and negative controls. Every enforced model behavior must be communicated without revealing private answers. Freeze the approved evaluation contract before the full run; do not trim frozen evidence, cases or gates for savings. After a failure, name the failed invariant, reproduce it, identify the changed layer and rerun only affected checks plus required integration/regression gates. Reuse other evidence under the identity conditions in “Evidence, custody, and closeout.” Do not add a second reviewer for agreement, replay a broad suite after a prose-only change, or rescore a preserved failure under new rules. A second reviewer needs a distinct required mandate or a material unresolved dispute. Batch documentation, custody and board updates once per accepted candidate or necessary blocker transition; keep failed attempts intact. Finish when the authorized deliverables and required closeout are complete.

## The Dev→QA Pipeline (default for any wave that writes code)

1. **Plan only independent, disjoint parallel slices.** Verify ownership by file path and shared mutable state; sequence shared files, dependent integration steps, and work that can invalidate another slice's evidence. Reserve independent QA capacity before filling producer slots. Stacked work branches start from an integration base of QA-passed branches.
2. **Dispatch independent dev agents in parallel** with self-contained prompts: context, goal, acceptance criteria, ownership + explicit NO-GO paths, safety rules, verify commands, report contract. **Brief the property to restore, not the code change to make** — devs honoring the property over the prescription have out-reasoned the brief repeatedly, confirmed by QA. Tell agents to verify orchestrator-supplied facts and to STOP at ownership boundaries.
3. **QA gate (separate agent, fresh eyes, adversarial mandate).** QA executes its own scoped behavior checks once against the candidate it grades — *assert which tree the package resolves to first; the stale-venv trap has caught the orchestrator too*. Run all repository-required checks. Add adversarial probes and the relevant real-integration checks; expand to a broad suite when changed behavior or integration risk warrants it. PASS/FAIL per slice; FAIL blocks. "PASS with defects" is a normal verdict — rank and route them. When QA's mandate is read-only and its scope is the untouched remainder of the repo, run it CONCURRENTLY with dev — enforce with a written "you may not modify a single line of code, findings go in the report" clause. When two slices build against one contract in parallel, gate the SEAM explicitly, not just each side: diff the keys one side emits against the fields the other actually reads, and hand-check any field one side derives rather than receives. **Dispatch-side counterpart (2026-08-28b): when one slice DEFINES a new contract and another CONSUMES it in the same wave, the orchestrator pins the exact signature verbatim in BOTH briefs, the consumer tests against its own local double, merge order is definer-first — and the gate's headline mandate is the one live pairing neither dev ran (consumer through the real composition against the definer's real implementation).** **An error message names one instance; the catalog names the class.** Two dev agents independently reported a schema failure as "fails on table X" because that is what the error said; the QA gate queried the system catalog against the registry and found **four** tables with the same defect — and the generating loop had no `ORDER BY`, so *which* one errored first was arbitrary and fixing the named one would have surfaced the next. Where a failure is generated by a loop, a rule, or a registry, QA enumerates the population rather than trusting the first exception. **Spend the stronger model on the gate, not only on the build:** the same run's dev agents also mis-diagnosed a circular dependency as a one-way cascade, which the gate refuted from `grep`-level evidence. **Content seam (2026-09-18/19):** the seam gate applies to prose too — when one slice's output is a *pointer* to state another slice changes, gate the pointer after both land or serialize them; cross-page references in a parallel wave cite by heading text or anchor, never by ordinal, and the orchestrator greps `§\d` across the assembled tree before the gate, fixing the section drafts in the same edit as the assembled file.
4. **Fix rounds go back to the same dev** with findings verbatim, subject to Law 9: reproduce first, refute if refutable, fix what survives. Negative-controlled regressions, then re-gate. Grant cross-boundary ownership explicitly per round.
5. **Micro-rounds for small post-gate defects:** tight single-file briefs; orchestrator does the final verification itself instead of a third full gate — proportionate process beats ritual process.
6. **Integrate QA-accepted source before final integration acceptance.** Run the orchestrator's behavioral smoke on the real seams of that merged tree while QA and report preparation proceed on separate outputs.
7. **Merge queue:** parents verified, byte-identical tree both orders, exact operator commands, new env vars / operator steps named up front, order chosen so the trunk is never transiently self-inconsistent.
8. **Persist probe suites**; retire superseded probes explicitly when a contract deliberately flips.

**Evidence, custody, and closeout.** Reuse existing evidence only when code, inputs, toolchain, configuration, rubric, and relevant agent/artifact/service identities match the claim; otherwise rerun the changed behavior or real integration risk. After later metadata- or documentation-only changes, verify identity and integrity instead of replaying unchanged behavior. Consolidate artifact custody once per accepted candidate and retain the canonical receipt or receipts required by each task/attempt contract; preserve failed evidence and negative results unchanged, and batch closeout after acceptance. Limit the shared custodian to Git and ledger mutations; let QA and report preparation proceed on separate outputs. Report a blocker when first known with its owner and evidence, reuse measured timings, mark unknown time as unknown, and add no tracking system solely for the retrospective. A tool that writes a fixed body makes every post-write append invisible to its own receipt (2026-09-19): when the contract requires content the writing tool cannot emit, extend the tool or stop citing the receipt as integrity proof — never both hand-append and cite it. Stop when the explicit completion criteria are met.

## Research-Fleet Discipline

- **Join citations to successful exact-source retrieval and inspect the relation itself.** A discovered URL or polished source summary is not evidence of an inspected original. Require ledger joins for every citation and claim, distinguish full text from query extracts, and separately check whether each typed relation describes implementation, prior work, analogy or a proposal. Passing schema and count gates cannot supply missing semantics.
- **Check source lifecycle as well as retrieval.** An accessible regulator page may be withdrawn guidance. Record publication/modification date separately from access, verify withdrawal or supersession for load-bearing rules, and preserve the still-valid statute separately from a revoked interpretation. A filename or failed fetch is not evidence of source content.
- **Treat retrieval as a capability matrix, not a binary installation check.** Probe each required surface (search, scrape, map/crawl, parse, async agent) against a tiny live target; record auth, quota, and response-shape failures; retry once; then fall back per operation to the best primary-source path. One working endpoint does not prove the rest, and one exhausted keyless route does not make the installed authenticated connector unusable. Record cost per surface alongside pass/fail — one `firecrawl_scrape` of an x.com post billed 30 credits while a Substack page embedding the same tweet cost 1; briefs carry a per-call cost ceiling with the cheap corroboration route named, and say up front when screenshots return as URLs an agent cannot see (2026-09-18). In the claude.ai chat surface `firecrawl_agent` may never receive its approval: probe the agent surface first with a trivial prompt, fall back to orchestrator-run search-per-slice, and say so in the acceptance report (2026-09-17).
- **Encode derived deliverable contracts in the artifact, not only in prose.** For agent/opportunity nodes, gate a coverage matrix across workflow, typed input, artifact output, real tool/API seam, failure risk, accountable human gate, and live metric. A fluent description with missing graph edges is incomplete because downstream builders load relations first.
- **Freeze the shared research contract before dispatch.** Hash acceptance thresholds, schema, reservations, and shared builders; make those paths explicit NO-GO for every slice; compare them on each return and before trusting any gate. If an agent changes the contract it is graded against, quarantine that wave, restore the frozen contract, and re-run every affected check on a quiet tree.
- **A reviewer must reproduce the deterministic gate, not summarize another agent's verdict.** Gate output from the current files and frozen configuration outranks prose that normalizes weaker thresholds or reports stale counts. Require exact command, exact configuration identity, and exact violations; a fresh model that merely agrees with an agent-reported PASS is not independent QA.
- **Every numeric claim lifted from a wide table carries its column header; the author's own disclaimer is a required field for any headline delta; the verification legend is defined in the report contract (2026-09-07 harness landscape).** Two slices cited one row of a vendor table and drew opposite conclusions because one slid a column; a 4/5 pitch was built on the inference the paper explicitly refuses; "verified" meant *found a source* in one file and *foundation-administered* in another. Brief researchers to quote `column → value` pairs, to search each primary source for its own caveats (`not`, `should not be interpreted`, `limitation`) and record the sentence verbatim before scoring, and to carry one glossary line per synthesis: who verified, against what. The gate makes same-source-different-value a mandatory check.
- **"Undrivable POST form" is a claim; read every Form D; the orchestrator-authored deliverable gets the same fresh gate (2026-09-11 Acres/Arcee).** Read a search form's field names and attempt one URL-constructed GET before filing the gap (the "undrivable" Arkansas SOS search was a plain GET that resolved two contradictions in one request). For any private US issuer with a CIK, open every `primary_doc.xml` — Item 5 carries the only Tier-A revenue band a private company emits, and record the consolidation scope with it. The final gate on the orchestrator's own report caught a P0 arithmetic error and an unlabelled author-built table; "verify, do not trust" briefs inverted the run's founding premise.
- **Paper-class claims are retrieved through the Firecrawl Research Index, and the ledger locator is the source id (2026-09-10).** A web snippet or scraped abstract page is not a paper citation. Briefs declare which tracks carry paper-class claims (methods, results, benchmark numbers, clinical evidence, prior art); those tracks use `firecrawl_research_search_papers` → `related_papers` (at least one expansion pass, pool size recorded) → `read_paper` for every quoted number, and cite `arxiv:` / `pmid:` / `pmcid:` / `doi:` with the operation class (`search` = abstract, `read` = full-text passage). The gate rejects a paper citation whose locator is a URL or whose quoted number lacks a `read` passage. This is not `search` + `categories: ["research"]`, which returns academic *web pages*. The capability-matrix probe includes the four `firecrawl_research_*` surfaces; keyless quota is shared across the fleet, so pass the authenticated key through MCP config before dispatch.
- **Hard thresholds, shared data fields and cross-slice facts are deterministic pre-web checks (2026-09-14).** A numeric screen band needs a stated pricing convention (multi-day average or ±tolerance) in the brief, and QA re-prices on the prior session — three band decisions inverted on a +7.9% sector day. A refuted shared data field is a class defect, not a slice finding: parallel slices have no channel, so the gate greps the whole wave for every card that consumed it. When slices touch one fact from different angles (a catalyst one slice ranks #1 that another found suspended), the gate diffs them *before* spending web calls.
- **Vault-writing waves obey the page contract and register their output (2026-09-14/17/19).** Frontmatter is a closed vocabulary, not a description: read `cortana-vault/scripts/vault-contract.json` before writing any page. `type:` is a declared type — never a `legacy_types` value on a new page; a page under `research/` is almost always `type: topic` — the first `tags:` element is exactly one declared emoji and no other emoji appears, and the six required keys (`title`, `type`, `created`, `updated`, `tags`, `status`) come first. If no declared value fits, use the closest declared value and say so in a `## Frontmatter note` line; proposing a new type or emoji is an operator decision for `_inbox/vault-dispositions/`, never a page's frontmatter. A template a slice authors is checked before any page is written from it. The wave's final gate is `scripts/vault-lint.py` scoped to the new folder, treating `legacy-*` warnings on pages authored in that run as failures. Before a research wave is declared complete, append a `## <Wave title> — <YYYY-MM-DD>` section to `research/research-hub.md` in the house form (wikilink row: folder index · brief · QA gate; what was produced in numbers; the standing quoting caveat), add the hub to the wave folder index's `related:`, and bump the hub's `updated:`.

## Operator-Runbook Execution (orchestrator-performed production changes)

When the operator gates open a runbook (data cutover, credential rotation, infra change), the orchestrator may execute it directly — held to the same standard as a code wave:

- **Discover live state before following the plan.** The plan's dominant risk may have expired: a cutover runbook built around in-flight-run safety was executed in a window where in-flight = 0 because the scheduler had been off for a day — discovered, not scheduled. Conversely a mid-sequence surprise (the fail-closed build deployed *before* its data migration, refusing readiness for 28h) can be benign, self-announcing state rather than an incident — a fail-closed design makes deploy-order mistakes survivable, which is part of why you build them.
- Execute gate by gate, asserting each postcondition in a *separate statement* (data-modifying-CTE snapshots lie about their own effects), recording preflight numbers before the change and comparing after.
- End with the Law 3d ladder: live traffic through the new path, source frozen, and an audit row recording what was done and why.
- Restore ambient state you changed for the probe (re-disable the job you enabled), and say so.
- **Qualify database authority as an exact identity tuple (2026-09-06 cutover).** A successful statement under a similarly named role does not prove production privilege: record and assert the actual LOGIN, role memberships, grantor, admin option, `INHERIT`, active role after `SET ROLE`, security-definer owner/search path, and the expected ACL as set equality; exercise positive and cross-workspace negative controls through the connection path production uses.
- **A deployment proves only the command it ran.** Read back the service's current source, exact commit, Dockerfile, root, start command, healthcheck, variables by name, volume mount and auto-deploy flag before release; a maintenance container or provider `SUCCESS` record is not readiness — acceptance begins when the full production factory boots and its real health endpoint and worker loops prove current behavior. Verify the public domain target against the actual listen port in startup logs (internal health on 8080 while the domain routed to 8000 and returned 502), and compare a monitor's stale threshold with the producer's actual cadence before deploying it. Run probes inside the real production image before claiming they are executable there (a slim image may lack Git); a host-generated commit manifest verifies mounted source only if the verifier checks every blob and exact file-set equality.
- **Keep credential planes separate.** A connector credential, an interactive browser session and a deployable runtime secret are different capabilities — never infer one supplies another, copy OAuth material between them, or place secrets in evidence; provision runtime credentials through the provider secret manager, verify presence without printing values, and test the application seam independently. A live alert-mapping test proves transport and persistence only: pair it with separate authentic control-origin tests, keep occurrence, persistence, delivery, acknowledgement and escalation timestamps distinct, and never let automated operator credentials stand in for human response.

## Detached hermes-agent Fleets (mechanics earned 2026-08-22/23)

- **Budget is finite and invisible:** `hermes chat -q` defaults to ~90 tool iterations — long slices die mid-flight *without reports*. Pass `--max-turns` sized to slice scope at launch, and resume dead sessions (`--resume <id>`) rather than redispatching: resumed context beats a cold restart.
- **Namespace runtime artifacts per sprint, and assert before spawn:** a launcher pointing Track B at last sprint's same-named brief cost a full false-start wave (the agent diligently worked the WRONG queue). Launcher asserts `[ -f "$BRIEF" ] || exit 2`; report paths carry the sprint suffix too.
- **Harness-tracked background watchers get reaped (SIGTERM); nohup+disown agents survive.** Don't arm fire-and-forget watchers — poll inline in foreground windows (sleep loops ≤600s per call), or you learn nothing when the monitor itself dies silently.
- **A shared checkout makes mid-wave suite runs LIE.** Another track's concurrent `tsc` swaps `dist/` under a running suite → phantom reds that vanish on a quiet tree (four phantom failures burned a diagnosis cycle this way; a zombie build-poll loop was the culprit — kill it, it burns budgets AND poisons verification). Certification requires the graded tree, shared build outputs, and relevant processes to be quiet; unrelated agents working on disjoint outputs need not stop.
- **Board discipline stays operator-shaped:** In Progress at dispatch (batch save), evidence comments at completion, In Review on verified-done, never Done. Verify each state change with a filtered re-list, not trust of save responses.

## Multi-Agent Git Mechanics (macOS/FUSE-mounted repos especially)

- **In multi-worktree waves sharing one `.git`, agents NEVER use `git stash` and NEVER run `git worktree prune` — put both bans verbatim in every dev AND QA brief.** `refs/stash` is a single shared ref: two agents stashing concurrently cross-talked and momentarily reverted a third's tracked-file edits (2026-08-28). And a prune executed from a sandbox that cannot see host-side worktree paths deregisters ALL of them — one QA prune silently deregistered ~26 host worktrees (files intact; recovery `git worktree repair <paths>` on the host). Corollary: **a standing operator rule that lives only in the orchestrator's head does not bind agents** — the orchestrator knew the no-bare-prune rule and omitted it from the QA brief; transcribing every known landmine into each brief is part of dispatch, not optional context.
- One worktree per agent; the shared checkout is contended and mounts may block `unlink` (stale `.git/*.lock`: delete the specific lock, retry once). On some FUSE mounts *any* index-touching git command — including `git status` — leaves an undeletable `index.lock`; use `find`/`ls`/`diff` for state and let the operator own every git invocation there.
- Stage explicitly per path — never `git add -A` in a shared tree.
- Rescue stranded commits via `git bundle`; verify the ref updated.
- Committing into a device-bridge/FUSE-mounted repo: mind the two-path namespace (mount path for the shell vs raw device path for file-transfer tools), per-repo git identity, scoped lock-sweeps that don't turn `refs/heads/*.lock` into broken refs, push-is-deploy handoffs, and verify-the-exact-SHA closes.
- After every wave: remove ALL worktrees including QA's via `git worktree remove` (each by name — NEVER `prune`, see the ban above; this line previously said prune and survived the 08-28 lineage merge as a live contradiction), shared checkout back to trunk, `git status` clean. Scratch branches QA created for merge-order probes get deleted; persisted probe suites get committed on a named branch first.
- Reconcile claims vs repo truth for every report: branch exists, tip matches, file list matches, protected paths untouched.

## Sandboxed-Shell Environments

- **Backgrounded processes do not outlive the call that started them** where each shell invocation runs in its own `bwrap --unshare-pid --die-with-parent` namespace. A server started in one call is gone by the next; on-disk state (data directories, extracted binaries) persists. Bootstrap scripts must be idempotent and re-run inside every call that needs the service, and any agent handed a connection string must be told this.
- **Root is often unavailable.** `apt-get install` fails; `apt-get download` plus `dpkg -x <deb> <prefix>` gives a working install rootlessly. Set `PATH` and `LD_LIBRARY_PATH` to the prefix and use a socket directory you own.
- **`unlink` may be blocked** in mounted user folders until delete permission is granted; write to new filenames rather than replacing in place, or request the permission explicitly.
- **Before freeing space under `/tmp`, list what the remaining gates need** (`CHROME_PATH`, browsers, caches): a deleted Chromium cache left a Lighthouse target unverified (2026-09-16). Decide the preview path before promising an artifact — in Cowork the first Artifact publish needs an approval card the session cannot show. Parallel Agent dispatch requires every invocation in one message. A session without vault filesystem access delivers a package + `PATCHES.md` (copy commands, index/log lines, verify steps) rather than claiming vault writes.

- **Give mutable verification fixtures an owner.** Agents testing the same preview server must use disjoint dates/accounts/records or separate data roots. Reserve shared browser fixtures explicitly; otherwise a legitimate compare-and-swap conflict can be mistaken for an autosave defect. Record intentional competing-client tests separately from incidental tester interference.

## Orchestrator-Performed Edits

Trivial mechanical changes are fine to self-perform — held to the pipeline's standard. Grep the whole tree for the old token after a rename; mark orchestrator-made commits as such so the next gate treats them with the same suspicion as agent work.

## Triage Before New Slices

When gates disagree or tests loop: stop generating slices, isolate the failing layer (static/config → runtime health → task delivery → tool lifecycle → canonical audit evidence → semantic response → gate truth → release decision), patch only the proven-defective layer. Cheap canonical evidence beats a rerun.

## Holdout / Eval Hygiene

Fresh-context grader, zero implementation context; orchestrator and producers never see scenario contents; results-only commits; scores back, never cases. Honor a NO-GO by filing remediation. What a reviewer actually grades may be a *docstring* — dead governance code with a confident docstring is what anchors them.

## Board / Tracker Discipline

- In Progress at dispatch; evidence comment + state change at completion; status update at wave end.
- File incidental findings as issues at the moment of discovery; batch minor ones into a themed issue.
- Evidence comments record what was *attacked and held*, what remains unverified, and refutations (Law 9) so disproved findings don't resurface.
- Operator-only actions become dedicated issues with exact steps. Placeholders get pasted literally once. No inline `#` comments in zsh paste-blocks. Credentials that travel SQL→URL→shell must be alphanumeric; test them at a psql prompt before they enter a URL.

## Reporting Format

1. **Current Verdict** — what is true now. 2. **Evidence** — commands, reports, gates, ids. 3. **Slices** — prompts / ownership matrix. 4. **Dependencies** — parallel vs sequential. 5. **Risks** — misreports, stale evidence, shared-file conflicts. 6. **Next Action** — the smallest useful move.

## Anti-Patterns

- More agents to compensate for unclear root cause.
- Two agents writing one shared file in the same wave.
- Accepting screenshots/agent comments without corroboration when release truth matters.
- Re-running expensive live tests before checking whether the last run already contains proof.
- Mixing runtime fixes, gate-truth fixes, docs, and release commits without naming scope.
- Burying "remaining defect" under a green headline — or a green system under an un-actioned defect list.
- Destructive action on an unverified orchestrator premise (Law 1) — or a fix built on an unreproduced QA premise (Law 9).
- Declaring done on a green suite when the production-touching artifact has never executed (Law 8). **Schema and migrations are the canonical never-executed artifact** — a green suite over an in-memory store is visually identical to one over a real database. **The boot/composition path is the second canonical instance (2026-08-28c):** a diff that touches app composition must be booted once with a production-shaped config and production-truthy env flags before the merge is greenlit — every `if is_production:` branch the diff can reach has to execute. Dev tests, QA probes, and a 4413-green merged suite all rode the dev factory while `create_runtime_app` under prod config raised at line one of the new code and crash-looped the deploy.
- Building a control in one slice and its call site in another (Law 11).
- Executing a runbook against the state it was written for instead of the state that exists (Operator-Runbook rule 1).
- Publishing a staged skill copy without checking it against BOTH the registry tip and the account store (see 2026-08-08c).
- Narrating a fleet, a write, or a gate that did not run — an unapproved agent surface, a claimed vault write, a green gate on placeholder media, a score without its runner config (2026-09-15/16/17).

## Self-Improvement Loop (when evidence earns a reusable lesson)

At closeout, review whether the run exposed a reusable orchestration lesson. Patch this skill only when evidence supports a new rule, materially sharpens one, or supersedes one; otherwise stop at the run's explicit completion criteria without a changelog-only update.

1. Ask: What moved the project? Which slices mis-scoped? Which reports/gates misled — including QA's? What did the environment do that code-reading couldn't predict? Did the orchestrator err, and would a written rule have prevented it?
2. Distill at most 1–3 crisp additions and fold them into the smallest relevant section.
3. **Patch this skill via the skill-publish gate** — never write the registry directly. Append a dated entry to `references/changelog.md` and stage the candidate in an isolated `_inbox/skills/_batches/<run>/orchestration/` directory so older staging remains intact. Before staging, diff the registry tip, every relevant staged copy, and each loaded harness target. Publish the exact reviewed bytes and sync the authorized harnesses when existing operator/session authorization covers publication; otherwise leave the candidate staged with the exact publish command.
4. Prune: fold never-fired or superseded rules into tighter laws; note consolidations. Sharper, not longer.

## Eval-Wave Rules (blind identification / grading waves)

- **The blind spec is itself a gate artifact — version it.** A transcription ambiguity in the orchestrator's sanitized spec (a candle-count/clock contradiction) suppressed calls two agents demonstrably saw; patch + bump the version the moment a wave exposes the defect (don't let a known-bad gate keep firing), and record which agents ran on which version.
- **Instruct agents to record suppressed candidates in notes.** Signals seen-but-vetoed become recoverable adjudication evidence; without them a spec defect is indistinguishable from agent blindness.
- **Rotate or overlap day/case blocks across agents.** A 0/4 block score is confounded with block difficulty unless another agent sees the same block; rotation is the load-bearing control for any re-test.
- **QA must not certify recoveries it adjudicated past a threshold.** A verdict that crosses the gate only via its own adjudication is a re-test trigger, not a pass.

## Changelog

Historical provenance and dated changes are in [references/changelog.md](references/changelog.md). Read only the relevant dated entry when investigating a past incident or reconciling lineage; do not load the history on routine dispatch. Operational rules remain above.
